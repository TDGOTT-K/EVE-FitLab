# FitLab 与独立引擎联合开发约定

- 2026-09-19 最新接入授权：用户交付D:/AI/GPT6/N号引擎-维护-0.201-r45/START-HERE.md并确认“开搞”；实际新副本0.210.0/r54。使用新独立状态目录进行接入，保留原r44运行数据，不自动迁移历史原生会话。原主库及历史副本仍不得修改。

用户于本会话授权在当前独立副本联合修改引擎与 UI。本文件记录持续有效的范围与验收规则。

- UI 仓库：D:/AI/GPT6/EVE-FitLab。
- 唯一允许修改的引擎仓库：D:/AI/GPT6/N号引擎-UI接入-0.190-r33。允许修改该副本的 Core、MCP、CLI、契约、测试及文档。
- 不得修改主引擎 D:/AI/GPT6/N号引擎、任何其他历史副本；不得自动升级或用新交付覆盖当前本地补丁。
- UI 展示的业务数值及必要上下文，必须可以在无头状态通过公开 MCP 和 CLI 获得同口径结果；涵盖分项、选定集合合计、基准差量、比例、曲线采样、业务峰值/参考条件等。现有不合规处见 docs/engine-v2/joint-development.md，不得宣称已全部完成。
- 游戏规则与数值公式由引擎统一实现。UI 可做契约允许的展示聚合、单位换算、舍入和图形布局；不得成为独有计算入口。展示聚合必须能用公开契约输入复现，不能只有 Python 内部函数可调用。
- 外部数据保留供应者、原始标识、时间、适用范围、缓存/缺失状态及可用时的内容哈希，并提供公开无头访问方式；网络失败不得虚构默认价格或其他业务值。
- 必须保留 null/state/reason、完整性、选定范围及未计入项。完整选集不等于完整战斗；备用潜在输出不得混入部署输出。
- 引擎和 UI 分别 Git 提交。接口变化须记录版本、冻结契约兼容性、旧输入/回执处理及验证证据；不修改旧绑定来绕过检查。
- 引擎本地补丁登记在该副本 docs/ui-local-changes.md，UI 迁移/验收登记在 docs/engine-v2/joint-development.md 和 nengine-ui-acceptance.md。升级前逐条确认补丁已上游合入、需要重放或被替代。
- 每个业务接口验收包含同输入 MCP 与 CLI 对照、边界/不可用/部分结果，并检查 UI 仅按契约展示。运行相关专项，不因普通改动反复跑引擎全量。
- 脚本控制的战斗模拟沙盒仍必须与用户共同设计，此授权不改变该约束。

- 最新范围补充：不开发引擎缺失的游戏机制，仅利用已实现机制；允许补公开接口、查询组合、上下文、诊断及UI适配。未支持机制保留明确不可用原因，不以新公式模拟补齐。
- 2026-09-18 最新用户补充：当前为测试阶段，无实际用户。不要求旧装备、旧配置、旧数据兼容；后续按当前引擎契约实现，不投入历史格式迁移或兼容层。此项取消历史数据兼容验收要求，但不授权删除用户现有数据，不改变主引擎/历史副本禁止修改的边界。

- 2026-09-19 UI约束：右侧属性栏整体以旧引擎最后版本0bd7823为设计基准，包括常驻行、顺序、标题、悬浮层与交互。不得以接入新引擎为由自行增加独立控件、面板或入口；确实需要新增的先向用户说明具体方案并确认。缺失值在原位置诚实显示，不用新入口替代原设计。

- 2026-09-19 当前联合接入副本已经改为D:/AI/GPT6/N号引擎-整合-0.200-r43（用户交付整合版本后继续联合联调），本地派生0.200.1-ui.1/r44，补丁UI-LOCAL-009。前述仅0.190-r33的路径为历史范围，不再指向当前接入。不得修改源主库或其他历史副本；整合副本补丁台账位于其docs/ui-local-changes.md，升级前必须逐项核对。
