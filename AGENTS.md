# FitLab 当前开发约定

- 本整理副本是允许修改的工作区；原始 FitLab、源主引擎及历史副本保持只读。不要迁移或删除原用户状态。
- 唯一当前默认计算引擎为相邻 `../NEngine` 的独立副本，环境变量 `FITLAB_NENGINE_ROOT` 可覆盖。校验 `UI-LOCAL-BASELINE.json` 优先，其次 `UI-BASELINE.json`；不得跳过校验、自动升级或覆盖冻结契约。
- 游戏规则由引擎公开 MCP/CLI 提供。UI 仅作契约允许的展示聚合、转换和布局，保留来源、null/state/reason、部分结果与未计入项。
- 引擎、UI 与扫描器分别提交。接口改变要记录版本、补丁来源和相关 MCP/CLI 验证；不因普通整理反复执行全量战斗测试。
- 右侧属性栏沿用既有布局；本次整理不授权重新设计 UI 或新增游戏机制。脚本控制战斗沙盒仍需与用户共同设计。
- 当前无历史数据兼容迁移要求，但不授权删除用户现有数据。
- 根 README 和 docs/README.md 为当前入口；engine-v2 的历史验收记录不作为当前路径配置。工作区清理不得删除仍使用的共享展示组件或来源/契约证据。
