# 工作区整理记录（2026-09-20）

## 来源与范围

在新的 `FITLAB-GitHub-Clean-20260920` 父目录中整理四个独立仓库。FitLab 保留原 Git 历史及复制时的未提交代码/翻译改动；当前引擎来自原维护派生副本，实际 0.211.0-ui.workbench1/r59。未修改原主库、原运行目录或用户数据。

## 已完成

- 删除 276 个废弃/重复文件，逐项路径与原因见 [机器清单](cleanup-manifest.json)。包括旧 Dogma 源码、.NET 9 宿主、旧打包工具、一次性补丁/探针、无运行调用的旧 Python 计算链及专属测试、重复官网和无引用数据样本。
- `run.py` 和 `server.py` 仅使用 NEngine；显式选择 legacy 会报错，不再暗中运行旧算法。保留当前仍使用的情景输入校验。
- 默认引擎位置改为相邻 `NEngine/`；新 `scripts/start.ps1` 使用相对路径和独立 `state/`。打包也读取同一位置或显式参数。
- 重写中英文 README、桌面说明、贡献约定、AGENTS 和文档索引。官网只保留 `website/`；历史下载未发布新版本。
- CI 安装实际依赖并执行当前 UI/语言检查，不再构建已删除的 .NET 9 宿主。
- 为曲线 schema 测试声明 `requirements-dev.txt`；测试读取基线 `contractDirectory`，不再猜测 `r59/` 路径。属性展示测试使用 jsdom；旧静默降级测试改为显式无效状态拒绝。保存测试检查标准化结果同时确认原草稿未变；SSO 回调测试使用临时端口。

## 特意保留

`engine-view.js`、`share-image.js` 等还有当前入口调用的展示/分享能力，不能因为名字旧而直接删除。`locales/legacy-aliases.json` 仍参与七语言转换；`data/full-catalog.json` 仍供应导航结构。它们不再是旧计算后端。

NEngine 的历史冻结契约、原始和本地两份基线、补丁台账、来源与缺口证据全部保留。旧设计文档标为历史参考，不把当时验收等同于当前全部通过。

## 验证

- 浏览器 81 个业务模块导入/变量绑定检查通过。
- 六套译文各 1902 项，与简中 source 的键、参数及重复项检查通过；七语言 i18n 专项通过。
- 服务端、角色、存储与情景校验：32 项通过。
- 桌面资源暂存成功，读取当前 0.211.0-ui.workbench1 基线；未构建或发布新安装包。
- NEngine 源码与冻结契约 475 文件及两份基线与原来源一致，未改游戏规则。
- 装备扫描器：97 项，96 通过、1 项原生集成测试未启用；仍锁定 r44。
- 动态扫描器离线：foundation 24、discovery 11、suites 52、fuzz 16，共 103 项通过。
- 动态扫描器原套件首次失败源于缺失原 `out/` 证据；34 个固定历史证据测试模块移入 `tests/evidence/`，原断言保留，未声称该组通过。
- 最终阶段门禁全部通过：94 项 Python、10 个 JavaScript 专项及 81 个 UI 模块检查。日志：`output/phase1-gate-20260920T132241Z/result.json`。首轮遗漏校验导入、缺失 schema 依赖与旧断言、第二轮旧契约路径的失败日志均保留于本地 `output/`。

## 上传边界

四个仓库各自提交。源代码不含原状态、输出、SDK、node_modules、编译程序集或 SDE 大文件。本地附带的运行文件受 Git 忽略，其他机器按 README 准备。

尚未 push、删除远端仓库、重写历史、部署官网或发布安装包。装备扫描器升级 r59、动态全机制复验、实际桌面安装和真实账号 SSO 属于后续工作，不能由这次整理通过替代。

本次实际验证环境：Windows、Python 3.9（独立 venv）、Node.js 22.16.0；文档推荐新开发环境使用 Python 3.11+。当前核心文档的本地链接检查通过；源文件无大于 50 MiB 的单文件，常见 GitHub token/私钥标记扫描未命中（不等同于完整秘密审计）。
