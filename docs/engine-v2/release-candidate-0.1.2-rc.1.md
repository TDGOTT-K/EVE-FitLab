# EVE FitLab 0.1.2-rc.1 测试发布候选

状态：本地冻结，待用户验收；未上传、未正式发布。

安装：运行 EVE-FitLab-0.1.2-rc.1-Windows-x64-Setup.exe。Windows 10/11 x64，包内自带运行时和静态数据。
本候选沿用正式应用的数据目录；如需完全隔离试用，可在 PowerShell 中设置 FITLAB_TEST_ROOT 为一个新的绝对目录，然后启动 win-unpacked/EVE FitLab.exe。不要用 --smoke-test 进行个人试用，它会创建测试装配并自动退出。

## 冻结来源
- UI：37b1c27495b7966a514962bab4e59efabb2dbc32（本地标签 v0.1.2-rc.1）
- 引擎：dfcbdb09a374cc7eb932bd01faa35f7c00dc847a（本地标签 fitlab-v0.1.2-rc.1）
- 引擎版本：0.210.4-ui.perf1；契约 r57-ui-affixes1；静态规则v59；SDE3503375。
- LOCAL-010～013已包含。完整来源和契约哈希见release-manifest.json。安装器SHA256见SHA256SUMS.txt。
- UI由Git提交归档后在独立目录构建，未包含未跟踪的草稿文档或网站logo。引擎使用当前独立副本已构建产物，不修改主工作区。

## 已验证
- 实际打包Electron启动，加载包内Python、.NET、SDE；两个内置角色，无个人装配或授权数据。
- 全V裂谷级真实分析、候选预览、安装提交、保存、撤销、重做、原生导出哈希一致；未授权HTTP请求403。
- 8项装配包/方案分享回归通过。包内公开契约文件哈希与冻结记录一致。
- 保存产生的是独立测试目录数据。详细运行报告validation.json。

## 验收边界与已知问题
- 尚未在干净虚拟机/其他电脑完成安装与卸载测试；本轮运行的是安装器同批生成的win-unpacked程序。
- 本轮未重新进行真实账号外部SSO登录。授权回调需要5207端口空闲；现有网页服务可能占用它。
- 未签名，Windows可能显示未知发布者提示。
- 首次新配置预览仍约1.3秒，外部估价可能更慢。部分机制仍保留partial/unsupported诊断，不宣称第一阶段全部完成。
- UI最终交互仍待你验收。建议检查装配库、新建/导入、安装/装弹、舰载机、角色/脑插方案、文字/图片分享、保存后重启恢复。
- 本目录和本地Git标签保留作回退基线；修改后另建rc.2，不覆盖本候选。未生成公开发布或自动更新动作。
