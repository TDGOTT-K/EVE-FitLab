# N 号副本接入与 UI 验收台账

> 当前已切到用户新交付的 **0.180 / r23** 独立副本，详见文末升级记录。下方第一批记录中的 0.166/r9 是历史基线。

日期：2026-09-18。状态：**第一批静态接入已实现，待用户逐项验收；不是全面迁移完成。**

## 隔离边界

- 原型 Git 检查点：`0bd7823`。
- 只连接 `D:/AI/GPT6/N号引擎-UI接入`，固定 `0.166.0 / nengine-headless-v1 r9 / SDE 3503375`。
- 启动时核对 UI-BASELINE.json、运行版本、契约修订及索引 SHA-256。没有更新、合并或修改 N 引擎代码。
- MCP 进程的状态目录在 FitLab 数据目录下；不使用原引擎工作区及其作业状态。
- 只开放静态分析和目录查询，适配层拒绝所有 battle_* 调用。脚本仿真、战斗沙盒、任务运行、回放 UI 均未设计或实现，必须与用户共同设计。

## 逐项验收

以下全部未获用户验收，不把自动测试通过当作设计通过。

| 编号 | 界面 / 改动 | 操作及预期 | 当前状态 |
| --- | --- | --- | --- |
| NUI-01 | 右栏与底部引擎来源 | 计算后显示 N 0.166.0 与 SDE 3503375；失败清除旧数值 | 已接入，待验收 |
| NUI-02 | 资源、槽位参数与悬停预览 | 装武器、装弹、启停、离线、过热；资源及参数来自 N；安装前校验挂点和数量限额 | 已接入，待验收 |
| NUI-03 | 舰体模式 | 战术驱逐舰选择模式后实际影响属性；缺少模式不偷偷默认；撤销恢复选择 | 已接入，待验收 |
| NUI-04 | 脑插与增效剂方案 | 应用方案后发送完整快照及每条副作用开关；影响真实计算；随机结果不会每次重算重抽 | 已接入，待验收；目录说明文案还需逐项核对新 SDE |
| NUI-05 | 真实舰载机浏览器 | 53 型真实目录；显示来源中队规模，原三个示例不自动变成真实装备 | 已接入，待验收 |
| NUI-06 | 发射管 / 备用机库 | 管数、轻/辅/重限额和体积来自引擎；安装、换位、交换、卸下、数量、参战均经过分析校验；保存/草稿/撤销保留 | 已接入，待验收；技能和未支持效果会阻止提交 |
| NUI-07 | 攻击显示 | 名义 DPS 不扣抗性；舰载机主武器分项展示；引擎 total=null 时显示 —，不相加伪造总 DPS | 已接入，待验收；火箭/特殊能力尚未加入静态控件 |
| NUI-08 | 电容 | 使用引擎平均负载稳定模型，绿/红显示；标明不是逐周期续航；不沿用旧周期模拟冒充 N 结果 | 已接入，待验收；原续航曲线和传/毁电情景尚未迁移 |
| NUI-09 | 防御、机动、锁定 | 不新增防御玩法；绑定 N 返回的 HP/EHP、抗性、机动和锁定值；针对抗来伤改变时重新查询 N | 已接入，绑定后的排版待验收 |
| NUI-10 | 不支持提示 | 当前副本遇到未覆盖效果时保留草稿和槽位；资源与攻击不补 0；展开“计算范围与待处理项”看原因 | 已接入，待验收；见阻塞 B-01 |
| NUI-11 | 情景入口 | 保留本地设置及切换；N 模式显式标记“情景未接入 · 显示基准”，不宣称效果已应用 | 尚未接入数值，不能验收为完成 |
| NUI-12 | DPS 曲线 / EDPS | 原交互代码保留；没有以旧公式加工 N 结果，新视图明确列为待接入 | 尚未接入 |
| NUI-13 | 分享图片 | N 报告暂阻止导出，避免旧导入码丢失新配置或图片画出错误数值 | 暂停此入口，格式迁移待办 |
| NUI-14 | 深渊随机变异 | 维持原明确标记的 mock；固定副本没有随机变异公开工具，不用本地随机公式冒充引擎 | 阻塞，正式接入后必须二次设计 |
| NUI-15 | Windows 启动 / 打包 | 宿主启动 FitLab 后端，由其管理副本 MCP；准备脚本只复制已构建宿主、运行时及固定索引，不构建/修改 N 源码 | 启动/打包代码已调整；尚未构建安装包或完成 Electron 验收 |

## 阻塞 B-01：完整全技能 V 输入

现有全技能角色的 505 项全部传入 `fit_analyze` 时，副本返回 `staticCoverageComplete=false`，`attributes={}`，DPS 为 null。不能声称装配校验通过，也不能静默删除这些技能后显示另一个角色的数值。

复现：`python -m unittest test_nengine_integration.NativeIntegration.test_full_skill_coverage_is_not_silently_filtered`。

本次观察到未覆盖来源包括技能 3322（660/661/662/668）、20210/20211/20212/20213/25718（1851）、37796/37797/37798/37799（6396/6679/6400/6401）。副本分别报告 `IMPERATIVE_EFFECT_ADAPTER_MISSING` 或 `DOGMA_DOMAIN`。这里只登记，不修补引擎、不改变用户技能。

无技能探针可以返回真实属性，但会带真实的 SKILL_REQUIRED 错误；这是数值探针，不是合法装配认证。后续由引擎负责者确定技能覆盖方案，再显式升级副本并复验。

## 接口与代码边界

- `nengine_bridge.py`：固定副本子进程、初始化、超时、序列化 RPC、契约校验、关闭；无 battle 通路。
- `nengine_adapter.py`：FitLab 装配到 EveFitRequest 的显式映射；技能不丢弃、模式/子系统/实例/副作用/舰载机身份保留。输出判别字段 `provider=nengine`、`contract=fitlab-analysis-v2`，原生报告保留在 native 中。
- `nengine_catalog.py`：读取同一副本的元数据，保留旧浏览器分类导航；不执行数值公式。当前导航目录并不等于新 SDE 全量物品覆盖。
- `nengine-view.js`：原生报告展示，只有格式化，不复制 Dogma、DPS 或电容算法。旧报告仍走旧 presenter，不伪造旧模型所需字段。
- HTTP：GET `/api/catalog`、`/api/fighters`、`/api/engine-status`；POST `/api/analyze`；原保存/角色/方案接口继续负责本地 UI 数据。
- 计算缓存现在包括整个装配输入；新增配置修改不会错误复用旧结果。失效返回值仍按 analysisVersion 丢弃。
- 本批未使用引擎会话事务替代 FitLab 本地保存、撤销与版本控制；这是后续重构项，不声称已完成 fit_create/fit_execute 全链路。

## 验证证据

- 8 项真实 N 副本集成测试通过（武器/弹药/技能/离线、模式惯性、脑插、真实药剂独立副作用、舰载机限额、不完整覆盖、情景边界、禁止 battle）。
- 31 项既有角色、方案、深渊本地存储、存储位置、工作区视图专项通过；这些旧测试在显式 legacy 模式运行，不等于 N 数值验收。
- 独立浏览器 958×912：真实型号装载、第四队轻型被拒、保存/刷新恢复、拖回卸下/撤销、完整全技能不回填数值且保留槽位。
- 截图：`output/nengine-qa/fighter-native.png`、`output/nengine-qa/unsupported-skills.png`；过程脚本 `output/nengine-ui-check.js`、`output/nengine-ui-limits.js`。
- 没有运行引擎全量测试、战斗作业，也没有构建 Windows 安装包；没有宣称完成这些验证。

启动：`python run.py`，默认 5208；可用 FITLAB_API_PORT 更换端口。FITLAB_NENGINE_ROOT 仅接受独立基线副本。旧后端仅保留为显式 FITLAB_CALCULATOR=legacy 的回归入口，绝不在 N 计算失败时自动回退。

### NUI-06 补充：已装载图标

发射管与备用机库的中队图标改用型号 typeId 对应的 EVE 实际图片（与浏览器同源），移除类别 SVG 占位。图片禁止独立拖动，保留整行拖动交互。待用户视觉验收。

### NUI-06 补充：舰载机右键菜单

- 浏览器：装入选中/首个空发射管（有占用时明确显示替换）、加入备用机库、详细信息。
- 已装中队：参战/待命、移入备用机库或装入空发射管、在浏览器中定位、卸下、详细信息；空发射管可右键选择舰载机。
- 复用装配界面的光标附近菜单、边缘避让、Esc、键盘导航及 Shift+F10；拖动时右键仍只取消拖动。
- 操作继续经过 N 副本校验与本地撤销。无可用计算结果时禁用安装，但仍允许查看详情。
- 补齐固定索引中舰载机类型的物品详情查询，没有修改引擎代码。
- 独立浏览器已验证：浏览器详情/安装、中队待命、移入备用/重新装管、卸下/撤销、键盘菜单；截图 output/nengine-qa/fighter-context-menu.png。待用户验收。


## 2026-09-18：r23 交付接入与武器栏（待用户验收）

已阅读新副本 START-HERE.md、UI-BASELINE.json、docs/ui-handoff-0.180-r23.md 和 AGENTS.md。默认路径更新到 `D:/AI/GPT6/N号引擎-UI接入-0.180-r23`；引擎源码没有修改，也没有更新旧副本。用户原始装配输入重新送入新版静态分析；状态目录新建为 FitLab 数据目录下 `nengine-ui-r23`，没有复制旧引擎会话/检查点或改写其版本字段。

真实 discovery 核对 0.180.0 / r23，索引仍为固定 3503375；适配器 source.revision 改由实际基线读取。Windows 准备脚本和启动路径同步更新，未制作安装包。

**NUI-16：中队右键菜单顶部武器栏**

- 使用该中队引擎返回的能力名称与来源属性识别主攻击、火箭和牺牲攻击；移动/规避等能力不混入武器栏。
- 暗影显示“脉冲加农炮”和“崇高牺牲”。前者可开关，后者在静态接口没有分项 DPS，禁用并标注“待接入”。r23 的战斗能力增加不等于静态接口已经返回这些 DPS。
- 开关保存到中队的 `excludedAbilities`，与参战状态、成员数量、管位和库存独立；保存、移动、撤销保留。
- `fighterDamageSelection.primaryDps` 仅汇总用户所选的引擎主武器分项；原生分析和请求原样保留，没有在 UI 新造伤害公式，没有通过收回中队假装关闭武器。完整总 DPS 仍遵守原生不可用标记。
- 菜单选中以绿色提示；待命/备用的选中状态显示“已选 · 待命”，不谎称当前正在贡献输出。
- 9 项真实新版适配专项通过。浏览器验证主武器关闭后分项为 0、参战/管位不变、特殊攻击禁用、保存刷新恢复及撤销。截图 `output/nengine-r23-qa/weapon-menu.png`，过程脚本 `output/fighter-weapon-check.js`。

新版交付纳入后续计划：下一批根据 discovery/schema 接入 mutation_rule、mutation_roll，保留生成回执，生成不自动安装；补充实例适配与二次 UI 设计。新增舰载机战斗政策、成员服务及仿真控制暂不做 UI，遵守必须与用户共同设计沙盒的约定。

全技能 V 覆盖阻塞在新版专项中仍可复现；情景、EDPS/曲线、分享等未完成项没有因切换版本被标为完成。未运行引擎全量或战斗作业。


## 2026-09-18：NUI-17 深渊真实生成与装配（待用户验收）

范围：高/中/低槽模块，使用固定 r23 副本；引擎代码未改动。仍无战斗沙盒 UI。

逐项验收：
1. 深渊工作台：真实适用突变质体下拉，替代演示三档；变异一次调用 mutation_roll，保存独立于安装。
2. 属性条：色带表示合法变异范围，虚线为基础值，白点为生成值；展示负数属性、原值在合法范围之外、改善/下降。优先使用规则的 highIsGood，再取固定属性元数据，未知方向不强判。显示取舍入值，保存完整精度。
3. 深渊库：实例显示“引擎变异 · 可安装”；旧 mock 保留且不能安装，打开后可重新生成。右键编辑/复制/删除，双击或 Enter 安装，拖到指定槽位或舰船图片安装。
4. 装配预览：悬停/拖入使用真实 mutation 计算，与普通模块共享预览、限额校验和撤销。仍显示真实技能要求/覆盖限制，不保证每个生成实例在任意装配都可计算。
5. 快照：装配保存 resultTypeId、mutation、实例名称和来源 ID；库编辑/删除不会改变已安装副本。换成普通装备清除变异字段，刷新、槽位移动、卸载、撤销保留正确状态。

接口：GET mutation-options；POST mutation-rule/mutation-roll；保存接口保留完整 generationReceipt 并向引擎按种子复算核验，不接受伪造属性。mutation 本身只包含原生契约字段，不混入 seed。UI 发现表来自固定 dynamicItemAttributes；只列出当前目录可装配的模块，未扩到变异无人机、货舱实例库存。

验证：14 项 Python 专项通过（新变异、旧实例存储、静态适配）；3 个 JS 模块语法检查通过。独立 5216 浏览器验证真实生成/保存、悬停预览、双击安装、拖入另一个槽、保存刷新、拖回浏览器卸载/撤销、普通模块替换清理/撤销。数值测试使用无技能角色，因此保留 SKILL_REQUIRED 提示，未声称合法配装通过。截图 output/abyssal-native-workbench.png；没有注入用户的装配或实例库。

本轮完成真实接口接入后的第一版二次设计，尚待用户逐项确认。后续引擎交付带来属性范围、支持类型或生成语义变化时，需要重新验证和相应调整 UI；不能沿用 mock 验收结果。


## 2026-09-18：NUI-18 r24 分项输出接入（待用户验收）

基线：独立副本 `D:/AI/GPT6/N号引擎-UI接入-0.181-r24`，0.181.0 / r24，23 工具，固定 SDE 3503375；已读 START-HERE、AGENTS、UI-BASELINE、升级说明、output-contributions 和八组 examples。保留现有深渊 UI 与突变质体图片。默认桥接与 Electron/准备脚本均改用新路径，FitLab 建立全新 `nengine-ui-r24` 状态目录；未复制旧检查点，没有修改任何引擎源代码。

待验收 UI：
1. 攻击区使用引擎 outputContributions 的已选读数，不再让旧 nominalDps 的舰载机总口径政策遮蔽主武器。默认只选舰载机主武器，保持原开关含义。
2. 输出口径下拉：名义周期 DPS / 有限弹量周期 DPS。后者注明假定可发射、非持续输出，均不扣抗性。
3. 中队右键武器栏：有限火箭在有限弹量口径下可勾选；保留选项后切回名义口径，会明确给出部分小计与火箭未能计入原因。没有可用周期读数的特殊能力仍不可擅自计入。
4. 输出分项折叠区显示已选/待命/未选择、不可计算原因。主值取引擎 selection.total，须 completeSelection；部分值取单一 groups.subtotal 并标“部分”。多组不强加。空选择遵守引擎 null，显示“— / 没有已选输出项”，不填 0。
5. 备用中队/无人机按 source.deployed 排除；UI 开关只改变显示选择，不改原生配装、部署、能力或库存。选择保留在原装配本地字段，可保存、刷新、撤销。

实现边界：普通武器、无人机、舰载机分类读数仅对同 metric/unit/非空 aggregationKey 且 state=available 的值聚合，未知不转零。主合计由现有 fit_analyze + context.output.selection 获取；没有为组合创建引擎新接口。原始分项、attributeKeys、application、constraints 保留在 native 结果中，过程解释/情景目标应用尚未制作本轮 UI。长期 sustainedDps、实际平均 DPS 和仿真战斗沙盒均未擅自设计。

验证：14 项既有专项（静态接入/深渊/存储）+4 项新分项集成测试通过；新测试比较引擎 reducer，并检查异口径/空值不可混合、备用排除、读数改变而 nativeFit/resources/fitHash 不变。真实例：六成员 Templar 主武器 219.375，含有限火箭 362.8125；名义口径包含火箭时 total=null，主武器保留小计。五组 mixed/mixed-applied/drone/bomb/sustained 使用交付输入核对，未运行战斗或全量引擎测试。

独立 5324 浏览器验证主武器/火箭开关、口径切换、部分状态与原因、保存刷新恢复、撤销；无技能浏览器夹具读数为117/193.5，保留技能要求提示，未冒充合法装配。截图 output/r24-partial-output.png。r23 已保存真实深渊回执在 r24 复算一致，未改写回执。用户库未注入测试数据。

### NUI-17 补充：突变质体选择器（待用户验收）

- 原生文字下拉替换为红黑图标列表，显示完整材料名、当前选中标记。
- 悬停或键盘聚焦时，旁侧显示固定索引的物品介绍与 ESI 参考 ISK 均价；介绍可独立滚动，价格固定可见。
- 价格标明获取时间及“非实时成交价”；缺报价、加载失败分别显示状态，不填 0。异步结果仅更新当前悬停材料，避免串项。
- 支持方向键/Home/End、Enter 选择、Esc 只收起列表、外侧点击收起；选择后更新图标并查询对应变异规则。
- 独立浏览器已验证六种大型护盾扩展装置材料图标、真实介绍/价格、切换后变异、键盘聚焦和 Esc。截图 output/mutation-material-picker.png。未改动引擎和用户实例数据。

## 2026-09-18：NUI-19 情景目标应用与 DPS 曲线（待用户验收）

- 情景继续绑定本地装配。目标从本地目标装配以 N 引擎重新分析，读取实际 ship/552 信号半径；画布距离、相对速度、角速度映射到 r24 context.output.target。不使用旧应用公式、不跟随目标自己的情景递归、不默默夹限速度；目标丢失/半径不可计算时明确报错。
- 攻击摘要、武器/无人机/舰载机已选分类、分项及中间装备行切换为 appliedCycleDps / appliedLoadedCycleDps，保留同一选择的 nominalCycleDps / loadedCycleDps 基准。完整有效数值增加绿、降低红；部分结果保留完整性提示，不能伪造完整总量。
- 悬停攻击摘要恢复距离/目标信号半径/角速度曲线，标签点击与 Tab 切换；当前坐标点与百分比、鼠标采样百分比、10 km 处角速度换算的横向速度保留。点值逐点调用真实 fit_analyze，无前端命中/伤害公式。
- 曲线按需加载，后端缓存绑定原生装配、查询 context、来源版本、目标参数；前端缓存也绑定目标实际参数和 fitHash，结果回来时不复活已关闭的悬停面板。每轴基础 33 点，并加入当前坐标与部分机制边界；不是连续解析函数或战斗时间曲线。
- 无情景时，尺寸可识别的舰载武器提供静止参考目标曲线：S=40m、M=125m、L=400m、XL=3000m。混合武器使用共同参考目标，取各尺寸参考与爆炸半径中的最大值，避免混合不同 target aggregationKey；其余变量归零并注明参考半径。没有尺寸映射的无人机/舰载机不猜参考级别，提示选择具体情景目标。情景已给出目标时可查询其引擎支持的分项。
- 目标抗性/EDPS、传电/毁电、电容受外界作用仍未接入，现有来源设置保存但不计入；通知说明电容仍为自身基准。导弹按引擎成功交付假设展示，不额外推断射程截止、拦截或飞行时序。未创建战斗沙盒 UI。
- 图表仅在所选集合完整时生成；缺政策的有限武器、炸弹不猜连发率或补零。炮塔应用期望可略高于名义周期值，遵从原生读数，不人为限制为 100%。

验证：16 项专项通过（静态接入9、分项4、情景3），确认目标半径来自引擎、目标丢失失败、nativeFit/资源不变、每条曲线当前采样等于工作区读数、部分输出不能成为完整曲线。JS 语法与 diff 检查通过。
独立 5325 浏览器验证：画布目标移动 10km→12km 后保存应用；红色数值、三轴曲线、Tab/标签切换、采样百分比和横向速度、不应用情景后恢复基准和理想曲线、重新应用及刷新恢复。截图 output/native-scenario-curve.png。无技能测试夹具保留技能要求提示，未称合法配装。测试数据未写入用户数据目录。未运行引擎全量测试。

## 2026-09-18：NUI-20 r33 基线迁移（待用户验收）

显式接入 `D:/AI/GPT6/N号引擎-UI接入-0.190-r33`，源提交 0efa261a7f0d1a5b53481a5d6798a1007faff980，副本 fe38afaf57b9faec7e5f0a811545068ff47df86c。已阅读 START-HERE、交付说明、缺口复核、AGENTS、UI-BASELINE 与 delivery-local-verification，并核对目标批量查询、舰载静态实体、电容情景说明。

实际 discovery 为 0.190.0 / r33 / 30 工具 / eve-static-dogma-v54 / SDE 3503375。默认桥接、Electron 开发路径、打包准备路径切到新独立副本；FitLab 建立全新 nengine-ui-r33 状态目录。未修改引擎源码、旧副本或历史绑定/哈希，没有复制旧检查点。30 工具发现不等于全部开放给页面，仍保持现有静态白名单。

本批变化：
- 全技能 V 原始 505 项输入全部保留并重新校验，r33 已可计算；更新旧“预期失败”的回归断言，不再把已修复问题当成缺口。
- 现有 DPS 曲线改用 context.output.targets 批量采样，同一次属性图服务多个点；按 256 点和 16384 贡献行上限分批，按采样 ID 匹配，绝不跨目标求和。
- 逐中队名称/能力读取 fighterEntities；新增 fighter_missile_primary 默认纳入主武器选择，并复用原有开关和撤销。辅助中队可保存/有来源参数不等于有伤害或目标应用；不把辅助输出填零。目录确认 53 个舰载型号，但没有声称 UI 完成全型号逐能力验收。
- 现有深渊、情景和分项 UI 保留。旧 r23 已保存的真实变异回执在 r33 复算一致，未重写来源。

验证：21 项现有专项全部通过（静态9、分项4、情景3、变异2、实例存储3），新增导弹主武器+支援实体专项1项通过；验证默认选择、关闭、原生配装不变及53型号目录。JS语法和diff检查通过。独立5333浏览器检查骑士I主武器菜单、关闭/撤销与r33版本。未运行引擎全量、战斗或Windows安装包验收；交付方的99项/协议/演示是副本交付证据，不冒充本UI任务重跑。

### r33 后续显式接入计划（尚未实现，逐批登记验收）
1. 固定目标层 EDPS：对接目标 Defense.Layers resonances，区分 DPS 与 EDPS；批量曲线用 effectiveCycleDps/effectiveLoadedCycleDps，保留目标层及所选集合完整性。
2. capacitor_scenario：只判断本舰；将画布传电/毁电来源绑定 sourceFit/moduleId，区分平均稳定、周期付款失败、有限注电耗尽与观察窗口结束，未知不显示为稳定。
3. booster_plan_analyze/roll/verify：替换现有本地随机与占位，独立方案不依赖伪造船体，保留坏分支和准入问题。
4. 普通库存/备弹/装弹量/实体晶体映射到原生inventory；动态变异备件不伪装成已支持。
5. catalog_type_details / fit_attributes / 结构化错误，补来源解释与可读诊断；市场树、离线图标另作版本化UI资源。

牺牲来源伤害与代价单独设计，不冒充循环DPS；目标应用与运行时继续按能力状态判定。以上计划不包含用户要求共同设计的脚本战斗沙盒。

## NUI-21：共享比较输出（待用户验收）

引擎本地97f59d7（0.190.1-ui.1 / local r34）新增公开分项/选集/样本比较；UI曲线和悬停消费其比例与差量，保留null及原因。图表百分比仅做ratio→%单位换算。原r33交付和主引擎未改。17项UI专项、当前点比例一致性及独立浏览器验证通过；截图output/local-r34-comparison.png。详细兼容性与未完成项见joint-development.md和引擎docs/ui-local-changes.md。

## NUI-22：已有固定层 EDPS 接入（待用户验收）

- 本轮不开发游戏机制，复用原有 effectiveCycleDps/effectiveLoadedCycleDps、target.layer 及本地公开比较字段。接口仍0.190.1-ui.1/local-r34。
- 攻击区恢复DPS/EDPS切换。情景目标的层由画布原护盾/装甲/结构操作决定，structure映射原生hull；从目标实际分析Defense.Layers读取resonances，并传递可用的ship/6186接收倍率。不把来袭伤害比例当目标抗性，不默认补接收倍率1。
- 返回scenarioTargetSource（原目标ID/名称/保存revision、fitHash、nativeFit、来源版本、选定层），保留重现目标数值所需原始上下文；来源信息在悬停明细显示。源头与攻击者原生输入可通过公开fit_analyze / sde-fit --metrics复算，不依赖UI伤害公式。
- 摘要、分项、分类小计、装备行和三轴曲线都按选择切换；差量/比例沿用同选集名义基准，曲线百分比仍相对抗性前名义总量。无目标时EDPS禁用并显示DPS，保留用户模式偏好供重新应用目标后恢复。
- 缺层effective读数保留null与exclusions；目标层本身不可计算时报错，不用默认抗性掩盖。固定层意味着不模拟层推进、维修、生存时间或实战平均。

验证：10项测试通过（EDPS2、情景3、输出5）；新增测试覆盖三层目标、原生配装不变、当前曲线点/比例一致、缺层不可用，以及同一原生fit/context实际CLI与MCP完整outputContributions一致。独立5341浏览器验证DPS10.61→护盾EDPS9.37、曲线单位/比例、移除情景回到基准10.56DPS。截图output/edps-shield-curve.png；没有注入用户库。总体功能仍未全部接入。

## NUI-23：独立药剂方案与原生抽签（待用户验收）

复用现有 booster_plan_analyze/roll/verify 及对应CLI，未新增机制、引擎接口或版本。
- 移除booster-roll.js本地Math.random抽签和对应旧测试，随机按钮及单药右键抽签调用原生无船体方案。每条副作用的概率/样本/是否生效、持续时间保留在完整回执。
- 方案增加明确“计算角色”选择，以技能快照保存，不绑定某艘船；脑插使用当前方案。副作用概率由引擎按技能/脑插修正，等待/不可计算显示省略号或破折号，不用基础概率回填。
- 手动副作用开关保留。最近回执独立保存，手动修改/角色改变后注明历史回执不再对应当前方案；核验按钮重放原回执，不伪造其证明当前状态。另存/复制保留历史来源。
- 保存回执时服务器调用原生verify拒绝篡改；种子限定JS可无损表达安全整数，宿主只生成种子不计算抽签。旧方案无pilot时明确无技能基准。
- 分开显示projectionComplete和角色先决条件。角色技能不足不吞掉可计算样本；是否能安装到船体由原有fit分析判定，不将独立方案当作合法装配认证。

验证：6项专项通过（原生方案2、存储2、分组2）。真实CLI与MCP同seed的完整结果一致、确定性重放、回执篡改拒绝、无船体、技能修正、手选、超安全整数拒绝、保存回执。独立5342浏览器验证全体抽签、保存、手动修改、历史核验；从无技能20%/1800秒切全技能V后15%/3600秒，保存刷新恢复角色快照与回执。用户库未注入测试数据。尚未完成全药剂/脑插种类逐一验收，不声明所有UI接入完成。

## NUI-24：普通库存与实际装弹量（待用户验收）

- cargo映射原生inventory.cargo，slots.loadedCharges映射magazines。字段缺失仍未声明；零为真实空仓，不默认满弹。已有物理crystals输入原样传递，实体编辑界面尚待完成。
- 弹药行原“满弹夹公式值”改为“装弹 未声明/数量”，点击以引擎投影读取容量，设置数量、选择满弹或恢复未声明。探测容量时仅构造只读零装弹候选，不写入草稿。
- 货舱占用/容量改读analysis.inventory，用原生十进制体积与拟合容量；装载弹药不重复计入货舱。超限保留原输入与诊断，不裁剪为合法数量。
- 换弹种/换装备/卸载重置不再匹配的装弹声明；装备整体移动保持快照，弹药交换带上明确数量，0和未声明不混淆；保存/刷新/撤销保持。
- 实际装弹是静态声明，不自动扣货舱、不冒充战斗供应；原名义周期/换弹假设不会因显式库存变成长期持续输出。
- 晶体不能变成普通堆叠，必须用实体身份与耐久。现阶段新增普通货舱入口明确拦截晶体/深渊类型，提示实体要求，不偷偷丢掉其身份。晶体UI仍待接（现有引擎支持）；动态变异备件为引擎未支持，不开发该机制。

验证：11项Python专项通过（库存2、静态接入9）；原生CLI/MCP库存结果一致，覆盖未声明/空仓/5发、160发容量、100发备弹0.25m³、两类超限保留诊断、晶体普通堆叠拒绝。槽位交换测试增加0/未知/明确数量传递并通过。独立5343浏览器验证实际数量编辑、保存刷新、恢复未声明/撤销；截图output/native-inventory.png。未执行全量或战斗，无用户测试数据写入。

## NUI-25：本舰周期电容情景（待用户验收）

复用现有capacitor_scenario/sde-capacitor-scenario，无新增机制或引擎版本。
- 情景画布的supportFitId/hostileFitId分别寻找已启用transmit/neutralize模块，绑定sourceFit/moduleId/distanceMeters；不在宿主重算载荷、周期、射程或电池抗性。来源名、保存revision、fitHash与完整原生请求保留。
- 默认满电、300秒、付款失败后停止消费者、fit_inventory供应、同刻来源ID顺序；可选60/300/900/3600秒窗口并随装配保存。输入明确记录于原生Query，CLI/MCP能重现同口径结果。
- 电容摘要区分平均稳定/平均不稳定/稳定性未确定，首次付款失败优先红色；展示已知净消耗、峰值回充、最低/结束GJ、首次归零/供应停止、付款结果。average.complete=false时不将knownNetDrain当完整稳定结论。
- 详情为原生电量样本曲线、来源与假设、平均排除项、各注电弹仓/预约量及共享备弹。共享货舱单独显示，不把私有reserve=0误作耗尽。窗口结束不冒充无限稳定；只判断本舰，不联算来源资源。
- 电容查询失败单独显示不可用原因并保留结构化diagnostic，不清空其他可用攻击/资源结果。原生CAP_SCENARIO_FIT/SUPPLY/NOS错误按code翻译，不解析英文推断规则。
- 当前没有NOS对方固定电量编辑UI；相关装配会显示缺条件，后续补已有能力的上下文输入。有限注电复用上批明确弹仓/货舱；不自动假设满弹或无限供应。仅支持静态电容，不涉及战斗沙盒。

验证：7项专项通过（电容3、库存2、EDPS2）；实际源绑定、CLI/MCP完整结果一致、来源丢失不显示稳定、共享有限供应average.complete=false、平均稳定但周期付款失败保留。独立5344浏览器验证毁电来源1km、首次归零60秒、曲线和60秒窗口保存刷新。截图output/native-cap-scenario.png。新增UI不等于全机制/全装配验收；NOS上下文、晶体与其他待办仍在总目标内。

## NUI-26：实体晶体库存（待用户验收）

- 复用现有 fit_analyze / sde-fit 的 inventory.crystals；无新游戏机制、无接口版本变化。
- 已装载晶体行显示原生累计损伤/耐久上限或“不磨损”；无实体声明显示“晶体未声明”，计算中显示“损伤待计算”。点击可声明实体或编辑损伤，也可选同型备用晶体。
- 物品浏览器向货舱添加晶体时改为声明独立实体；显式输入累计损伤（不是百分比）。原生校验通过后才写入草稿。货舱每枚晶体独立显示，右键编辑损伤、查看信息、装入兼容槽位或移出。
- 卸下、换弹及卸载装备把原实体放入货舱并提示；交换装备/弹药时身份随位置交换。替换备用晶体不复制或删除原晶体。撤销/重做、草稿比较、保存刷新保留身份与损伤。
- 数值完全读取引擎投影，JS模块仅处理库存身份/位置。预览使用克隆库存，不能提前移动真实物品。超限和已损毁/负损伤由原生接口拒绝；旧普通晶体堆叠仍明确不可用，不自动转成全新晶体。

验证：库存3项+电容3项Python专项通过，包含真实CLI/MCP库存完整一致、已装载不占货舱、备用占用、负损伤/达到耐久上限拒绝；JS身份交换/替换/卸下/未知显示通过。独立5344浏览器验证0.5→非法1拒绝→0.25、卸下/撤销、右键备用替换、保存刷新。截图output/native-crystal-stock.png。样本舰船故意保留技能/资源非法状态，仅验证库存，不声称为合法可运行装配。

待办：旧普通晶体堆叠的批量人工迁移交互、备用晶体直接拖入槽位，以及市场估价的实体数量口径。测试另确认旧UI目录SDE3248221与引擎3503375存在差异，旧server.validate_fit会拦截部分新技能；后续改为原生目录/校验，不删除用户技能来绕过。

## NUI-27：完整当前目录与新技能（待用户验收）

- 原refresh_catalog仅刷新旧列表中的ID，漏掉当前SDE新增物品。现在从锁定独立副本完整枚举已发布舰船、技能、弹药、无人机、子系统及带槽位效果的模块。类型/属性/效果/容量/体积/名称/科技组均来自当前索引，不再用旧目录字段兜底业务值。
- 内置全技能V从505变为511，新增舰载机专精/旗舰级分解者专精/指挥航母技能可选择和保存；新船/新装备可浏览与恢复装配。目录出现不表示机制已支持，分析仍保留原生覆盖与准入状态。
- 新增names优先供多语言名称/搜索使用；未知科技组显示“未标注科技分类”。启动状态不再固定写旧SDE编号。
- 明确使用UI-LOCAL-BASELINE（存在时），核对索引manifest与锁定哈希；每条记录保留类型与数据版本，/api/catalog-source返回来源URL、发布日、归档/索引哈希。新增catalog_type_details静态白名单，用于公开接口核验；引擎接口版本不变。
- 副本不包含marketGroups。旧物品沿用历史导航路径并标记navigationSource=fitlab-market-tree-sde-3248221；新物品用当前category/group名称分组。此为UI导航资源，未伪造当前市场树，统一当前市场树仍待后续资源接入。

验证：3项新增专项通过；分页MCP核对5类完整ID集合；6个新类型的名称/原始属性与公开MCP一致，真实sde-type-details CLI完整输出逐项等同MCP。新船91775+全部511技能通过原生装配校验，自定义角色持久化保持511项；伪装成技能的船体ID仍拒绝。9项既有接入测试通过（首次组合运行的新增目录测试因测试分页limit=200被契约拒绝，修正为100后新增3项全过）。独立5345浏览器验证新船、全技能、搜索92454、保存刷新；截图output/native-catalog.png。未跑全量或战斗，未修改用户测试数据。

## NUI-28：技能点缺能力状态（待用户验收）

检查当前副本Core、MCP、CLI与官网导入：没有技能点总量接口，官网导入未保存带来源SP快照。遵守用户不开发缺失游戏机制的范围，删除skill-points.js里按rank/等级估算总SP的独有公式。角色列表、角色详情、装配驾驶员选择器保留位置，统一显示“— SP”，悬停说明当前来源缺失；不将无技能角色特判为可计算0，不将等级估计值称为真实总技能点。

此项是明确不可用状态，不是技能点接口已实现，也不改变技能等级、技能数量或其对装配的作用。将来若副本提供既有接口或接入带来源官网快照，再独立验收；本轮不补训练公式。

验证：模块语法通过；独立5345浏览器刷新后角色列表与全技能V详情均显示“—”及完整原因。未变更引擎运行时代码/契约，未跑无关全量。此前页面旧模块未刷新时仍显示旧估计，刷新后已确认更新。

## NUI-29：静态吸电条件（待用户验收）

- 情景画布本舰右键“初始电量”，目标/敌方来源右键“固定电量 · 吸电条件”；用滑条和空电/满电快捷项调整，对方可恢复未声明。空白处右键也可进入本舰初始电量，避免近距离图标重叠；本舰支持键盘菜单。
- 本舰初始百分比仅换算为已有query.initialFraction；对方直接声明GJ。目标容量和能量抗性读取目标原生分析并绑定id/revision/fitHash/nativeFit/来源；本舰每个启用NOS传入nosTargets。对方装配不可用或缺边界时明确不可用，不补默认电量。
- 敌方来源现在纳入已启用neutralize和nosferatu，外来NOS传入sourceAmountGj，容量由绑定sourceFit决定；不允许宿主覆盖载荷/周期/射程。更换对象清除旧电量声明，避免沿用不同装配的容量假设。
- 显示实际初始比例、固定电量、吸电目标/来源。平均模型保持NOS排除和complete=false，不把条件收益当永久稳定；对方固定边界不联算双方资源。零GJ和未声明分离。无新游戏机制、无战斗沙盒UI。

验证：5项电容专项通过；本舰和外来NOS实际CLI/MCP完整结果相同，原生事件输入和正收益验证；缺电量/超容量/非法初始值保持不可用；情景保存保留0/null并拒绝非法值。独立5346浏览器测试目标未声明→250GJ、本舰10%、保存并刷新恢复；外来吸电未声明→0GJ保存有效。截图output/native-nos-target-control.png。JS语法及diff检查通过，未执行无关全量。

接口仍0.190.1-ui.1/local-r34。这里只对已有静态capacitor_scenario补上下文，没有给缺失机制添加实现。未声明时仍不会声称装配完整稳定。

## NUI-30：按需拟合属性与原生轨迹（待用户验收）

- 物品详情不再从主分析偶然访问过的attributes中截取不完整快照。对舰船、已安装模块/改装件/子系统、已装填弹药和无人机，使用现有fit_attributes按需查询公开属性。弹药使用charge.<slot>，修复原先错误映射到module.<slot>。
- 新宿主/api/native-attributes只负责UI装配映射与1–256条批次调用，返回原生items/traces/source/fitHash/errors/warnings/coverage及可直接重放的公开请求；合并时校验批次来源一致。无属性求值或游戏公式。
- 装配参数读取逐项state/trace，恢复此前被旧renderer排除的射速/射程/跟踪等字段。悬停展示baseValue、原生steps的来源/算子/输入/堆叠系数及前后值；色彩仅依据metadata.highIsGood，不推测好坏方向。单位与舍入只做展示。
- unavailable/requires_policy/blocked_static_dependencies显示原因，不转换成0；不主动允许默认值。校验错误保留诊断说明，不把读得到属性当作合法装配。属性查询失败仍保留基础属性页。原生路径不再走旧的DPS/弹药基础公式；旧引擎路径独立保留。
- 请求绑定打开详情时的装配快照；切换物品/关闭后丢弃过期响应，保留等待期间用户选择的标签页和滚动位置。

验证：3项Python专项通过：修正轨迹真实CLI/MCP完整相等、charge类型178、缺对象/null、默认值策略、257项分批/顺序及非法查询。JS测试确认blocked不调用数值格式化、真实0正常显示、未知原因保留及highIsGood方向。独立5347浏览器实测炮台23项、弹药11项；弹药所有对象标识为charge.high-0，射程/跟踪可见，CPU 3→2.25tf有技能轨迹，伤害倍率3.3654有原生修正。截图output/native-attribute-details.png。未执行全量或战斗；本批仍不等于全应用接入完成。

接口版本保持0.190.1-ui.1/local-r34，无引擎机制或契约修改。

## NUI-31：舰载机装配属性上下文（待用户验收）

- 发射管与备用机库的“详细信息”传入具体fighter.<中队ID>，接入既有按需属性路径；缺显式ID的历史中队使用与native_fit相同的fighter-tubes/reserve-序号身份。
- 保留浏览器未安装物品只显示基础资料的语义。已安装详情标明发射管/备用中队、单架属性与中队架数，不能把属性图数值当成整中队合计。
- 继承原生状态/轨迹/单位格式化；悬停补属性ID，区分官方同名属性。没有添加舰载机机制或改变输出选择、部署状态。

验证：4项原生属性专项通过（含之前CLI/MCP对照），新增发射管/备用/旧ID三种身份、错误装配仍可读诊断属性。JS空值/方向测试及语法检查通过。独立5347浏览器分别打开tube-alpha/spare-beta，均有48项属性且对象身份各自正确；能看到最大速度1301.5625m/s、护盾5132.8125HP、伤害倍率2.8125等原生加成及轨迹。截图output/native-fighter-attributes.png。没有重跑全量，无引擎运行时或版本变化。

## NUI-32：公开深渊比较与历史回执（待用户验收）

引擎提交3d41208，本地派生0.190.2-ui.1/local-r35，非官方上游r35。原交付与r33/r34冻结契约不变。
- 变异结果的差量、相对变化、改善/下降/不变/方向未知与计数现在由已有mutation_roll公开输出提供，CLI同口径。UI删除相对变化公式、改善判断和本地计数，只按结果着色及百分比单位格式化。
- 不再用属性metadata.highIsGood覆盖突变规则的未知方向；未知方向中性展示并计入“方向未知”。零基准相对变化为“—”，悬停说明原因。源范围数值仍来自原生规则；条带中的百分比位置只用于图形布局，不声称质量得分。
- 新生成回执保存新字段。历史回执经完整种子重放核验，只对历史缺失的新增派生字段做兼容比对；存在但被篡改的新字段拒绝。历史记录再次保存保持原receipt，页面通过mutation-review只读重放获取新比较，不回写历史。
- 宿主和Windows宿主显式切nengine-ui-local-r35状态目录，保持旧状态；读取当前本地基线，未修改原交付清单或主引擎。

验证：引擎9项变异专项、3项契约通过，独立5案例CLI/MCP/schema/算术对照通过（含原输出比较回归）。UI7项变异/属性专项通过，覆盖真实CLI/MCP、原字段篡改及新比较字段篡改拒绝、历史保存不改写。独立5348浏览器确认生成颜色计数与原生summary一致、保存、历史实例核验及再保存receipt完全不变。截图output/native-mutation-comparison.png；示例出现3项方向未知，这是原生规则未声明，不擅自判断。构建无警告错误；未重复全量，Windows本轮只做入口语法检查，未重新打包。

## NUI-33：曲线完整无头查询（待用户验收）

引擎dc5a2f3、验证14d1841；本地0.190.3-ui.1/r36（非上游发布），31工具。原r33/r34/r35冻结契约与原交付基线保留。
- 新公开MCP fit_output_curves / CLI sde-output-curves统一参考目标、三轴范围、采样网格、当前点、每轴及全局采样峰值、选集完整性与比较比例。UI nengine_curves仅翻译标签、映射结构，不再独自生成上述业务条件或结果。
- 无情景政策为weapon-size-ideal-v1；武器尺寸128对应40/125/400/3000m，混装取最大参考/爆炸半径，其他理想条件显式记录。缺尺寸（含部分火箭发射器）保持不可用，需要具体目标，不猜类别。显式目标固定其余条件；EDPS要求目标层。
- 引擎批次沿用256目标/16384贡献行上限，总采样<=4096，默认32区间并包含当前值和边界。图中峰值改称“采样峰值”，不冒充解析极值。每点保留selection/exclusions/comparison，未知不填0；10km角速度换算参考随公开输出传给页面。
- 宿主与Windows入口改用独立r36状态目录。未新增游戏机制；只组合已有静态输出能力。兼容旧查询，新增独立入口，不改历史回执。

验证：3项引擎曲线专项+3项契约通过，构建0警告错误；独立协议脚本8案例CLI/MCP/schema/算术通过，含三类曲线和既有输出/突变比较。UI7项曲线、情景、EDPS专项通过；独立5349页面验证无情景三轴、40m参考、37.67DPS采样峰值，固定装甲层当前19.227057251426533EDPS与原生结果相等。截图output/public-curve-policy.png。远程字体超时导致首次页面截图等待失败；隔离QA浏览器禁用远程字体后完成DOM操作与截图，产品代码未改字体。本批未做全量或Windows重新打包。

## NUI-34：被动装备安装与旧状态修复（待用户验收）

问题：磁性力场稳定器 II 被设为 Active，触发 EVE_CAPACITOR_EFFECT。目录与适配器错误地将非默认 online 效果 16（类别 1）作为启动能力。全目录确认 971 个模块类型受到该误判影响：低槽 727、中槽 218、高槽 26，包括磁稳、回转稳定器、护盾扩展、电容回充、电池等。

- 改用锁定 SDE 的唯一默认启动效果（类别 1/2/3）判断可启用；超载还须具有类别 5 效果。目录生成脚本同步修正，避免重建回归。
- 统一 Python 状态适配和前端展示，旧 Active 被动装备恢复为 Online；草稿、保存与新旧图片导入均兼容。保留显式 Offline 和真正主动装备的状态；无效状态、被动 Overload 仍拒绝，不掩盖引擎不支持的机制。
- 引擎运行时、公式、MCP/CLI、版本均未改动，仍为本地派生 0.190.3-ui.1/r36。主引擎与历史副本未动。

验证：6 项 Python 状态/全目录/库存回归通过；磁稳测试装配混合炮输出由离线 13.23 DPS 提高为在线 16.26033519553073 DPS，完整 fit_analyze 与 sde-fit 输出一致。JS 新旧图片恢复与离线保留回归通过。隔离 5350 浏览器验证旧 Active 草稿恢复、右键无启用/超载、卸载后双击重新安装、保存为 Online；字体网络失败与本项无关。5208 服务已重启加载新目录。未重新打包 Windows，未运行引擎全量。

## NUI-35：脑插与增效剂快捷入口修复（待用户验收）

修复 openLoadoutPicker 误引用方案管理器局部变量 extraFolders：已有分组（layout.revision>0）时会显示 ReferenceError，阻断方案选择。快捷选择器只读取 loadout-plans，去除无用的布局、角色请求及管理器状态写入；不改变管理页面分组或引擎计算。

浏览器验证：真实 5208 菜单能列出已有 21 个方案并应用“测试 01 · 护盾容量 · 入门”；隔离浏览器组件检查方案名/嵌套分组搜索、版本更新标签、选用回调和空库清除通过，仅请求方案列表。用户装配库未写入测试数据；未修改引擎及接口版本。本项为入口修复，不代表全部脑插机制覆盖。
补充验证：同一裂谷装配使用全技能 V 时，应用上述真实已存方案使原生护盾容量由 562.5 增至 675.1622042529723，电容周期查询 available。先前隔离浏览器使用无技能角色，周期不可用来自先决技能未满足，并非此次方案入口修复失效。

## NUI-36：脑插与增效剂物品详情改用当前 SDE（待用户验收）

脑插/增效剂右键“详细信息”此前单独加载旧前端 loadout-item-info.js，已改为与装备共用当前 /api/items/{typeId}。描述、基础属性、单位与效果定义均来自固定 SDE 3503375；响应新增 source（原始 typeId、表名、索引/归档哈希、下载地址、发布时间和范围），基础属性页标注实际 SDE 版本。不把基础属性视作当前装配计算结果。

验证：9941 脑插和 9947/9950/10151 增效剂的描述及全属性与 catalog_type_details 相同，四例 CLI sde-type-details 与 MCP 完整结果一致。浏览器右键详情路径确认请求 /api/items/9950、展示 SDE 3503375，不再加载旧静态详情模块；JS 语法检查通过。当前引擎 0.190.3-ui.1/r36 不变，无机制或接口版本更改。未打包 Windows。

仍待接入：方案管理浏览器的完整目录/分类与悬停介绍仍包含前端生成目录数据；已安装方案项的独立计算属性详情尚需明确上下文接入。本项只完成物品基础详情来源统一，不宣称脑插全链路已结束。

## NUI-37：当前脑插/增效剂目录与悬停介绍（待用户验收）

- 新 /api/loadout-catalog 从固定 SDE 3503375 枚举 published、类别 20 且具有有效 implantness/boosterness 槽位的物品；当前 838 脑插、453 增效剂。app、方案管理和脑插编辑器共用 loadout-catalog.js，不再使用静态旧目录作为可选清单。
- 名称、槽位、介绍、副作用效果 ID 和名称均来自当前 SDE；悬停内容摘录来源描述原句，无可摘录加成时保留原文。每条附 metadataSource，目录附来源 URL/发布时间/索引及归档哈希；原始内容可用 catalog_search/catalog_type_details 与对应 CLI 无头读取。副作用概率不由目录猜测，仍查 booster_plan_analyze。
- 将原增益分类规则提取到 loadout_taxonomy.py，基于当前属性生成导航标签；navigationSource 明确为 UI 分类策略，不是引擎计算结论。保留纯属性“白板”、有战斗加成的混合脑插不归白板、多作用多分类。

验证：四项 Python 专项通过，完整当前 ID 集与公开 catalog_search 对照，逐条悬停原句追溯，三种药剂副作用集合与公开 booster_plan_analyze 一致；JS 对当前目录的 55 套六件套安装/补齐/替换/不完整套装回归通过。浏览器验证入口、目录实际加载 838/453、极乐搜索及六件套；语法检查通过。仍沿用本地引擎 0.190.3-ui.1/r36，未新增游戏机制、未改变公开引擎契约，未做 Windows 重打包。已安装方案项计算详情、价格来源、分享图及事务等其他缺口仍未宣称完成。

## NUI-38：已安装方案项参数详情（待用户验收）

方案管理器已安装脑插/增效剂的右键详情携带打开时 plan/kind/slot 快照，查询公开 booster_plan_analyze，使用方案本身角色与副作用状态。新增“方案参数”页，明确独立方案参数不含舰船作用；浏览器未安装物品仍展示基础详情。界面沿用属性来源链展示和引擎 metadata.highIsGood，不计算游戏数值。

plan-attribute-inspection 只映射公开 Attributes/Issues/ProjectionComplete 和实例标识；缺少 trace 显式 unavailable，完整性失败时 blocked_static_dependencies，不用基础值回填。保留 planHash、规则版本和来源。当前接口保证枚举增效剂属性，并附递归依赖；没有完整枚举所有脑插属性，因此未返回的脑插项明确提示，尚未宣称完整方案属性覆盖。

验证：JS 使用真实 booster_plan_analyze 返回检查修改后来源链、原始 trace 引用、作用域、缺失值、未支持投影与不存在对象，未修改输入；2 项原生方案回归含 CLI/MCP 随机回执一致与角色/手选副作用通过。独立浏览器草稿选择生物学 V、标准蓝丸及副作用 2737，经已安装槽位右键打开，确认“方案参数”和角色/无舰船上下文。语法与 diff 检查通过；仅 UI 修改，本地引擎 0.190.3-ui.1/r36 未变，未重打包 Windows。

## NUI-39：脑插方案源属性与本地 r37 接入（待用户验收）

独立引擎提交113d3e0，本地0.190.4-ui.1/r37，31工具。复用现有计算图，公开booster_plan_analyze/sde-booster-plan增加已装脑插的全部显式源属性及依赖链；纯脑插方案现在可显示“方案参数”。未增加游戏机制，不声明独立方案有舰船作用。工作台与Windows宿主默认状态目录切nengine-ui-local-r37。

修复NUI-38记录的脑插枚举缺口；当前已装脑插和增效剂均走原生查询。引擎未返回的属性仍明确不可用，unsupported_static仍保留阻断，未作基础值或零回填。新契约冻结与基线、主引擎移交清单见副本docs/ui-local-changes.md LOCAL-004。

验证：20项引擎方案/增效剂专项、3项公共契约、3项UI方案测试通过；纯脑插公开CLI/MCP完整结果一致；来源属性与真实装配属性逐项对照通过。JS属性映射回归与浏览器控制论V、27147纯脑插槽位右键详情通过，显示方案角色、无舰船上下文和原生属性。服务5208已加载新副本；未重打包Windows、未重复全量。

## NUI-40（分享接入进行中）：当前配置导入码

现有EVF2字段集缺少新引擎关键输入，本批新增EVF3导出载荷：复用基本槽位/技能二进制编码，加显式白名单扩展，保留舰体模式、输出口径、观察窗口、来伤比例、脑插/增效剂选择、方案角色快照、深渊绝对属性、弹量（含显式0）、晶体ID/损伤/装填位置、无人机变异、舰载机中队和武器选择。未声明货舱与显式空货舱仍区分。只有输入快照，未添加UI业务公式。

不包含本地装配ID、方案库ID/分组/版本、账户信息、关联情景、随机回执。驾驶员名称遵循导出开关。导入仅恢复字段，游戏合法性仍交calculate/引擎验证。受原有16码、压缩/解压体积和校验约束，超限报错而非截断。不是新增历史数据兼容工作。

验证：现有二维码结构/损坏/多片测试通过；新增深渊、模式、舰载机、显式0、未知库存和隐私字段回归通过；真实含晶体、脑插/药剂的装配编码前后nativeFit及完整native结果一致。浏览器实际QR绘制→PNG→扫描→恢复脑插方案通过。图片原生数值渲染仍待完成，因此未解除新引擎分享按钮的临时限制，不能将本批视为分享功能已经完成。下一步渲染必须保留原生不完整/不可用状态及引擎版本来源，不使用旧字段公式。

### NUI-40 完成原生长图与导入链路（待用户验收）

已解除新引擎“生成图片”限制。native-share-image复用mountNativeStats展示器读取原生摘要、分项、状态、诊断和口径，绘制深色长图；不使用旧share-image里的EHP/DPS公式。列出槽位/状态/弹药、深渊绝对属性、无人机、脑插、药剂副作用、晶体损伤/装载、舰载机与武器选择；同型但不同实例不合并。图片移除情景，DPS不扣目标抗性，保留名义/有限弹量周期、观察窗口和引擎/SDE/接口版本。二维码始终保留完整输入，货舱开关仅控制可见清单。

原生估价来源与公开查询尚未接入，因此本次图片不显示估价，未沿用旧“满弹夹”估算；估价仍是总目标待办。复制/保存保留现有入口，远程图标超时保留名称和输入；长图超过高度上限报错不截断。当前详情为文字/数值快照，未把电容SVG曲线搬入图片。

导入calculate返回isValid=false时显示“可导入草稿”，不宣称合法。端到端验收还定位并修复refreshLibraryRows中的未定义next变量：原先保存成功但后续刷新错误，导致导入对话框不关闭。服务端启动状态目录补齐r37，与桥接器和Windows入口一致。

验证：真实1080×5965 PNG（output/native-share.png）包含511技能、已装晶体、脑插和选择副作用的增效剂；PNG扫描/恢复后nativeFit和完整native结果逐项一致，电容周期可用。另验证不可用电容文字保留原因；页面实际“生成图片”完成预览及保存按钮启用。隔离5351装配库通过文件选择、扫描、原生校验、保存新装配、关闭对话框与库刷新，保存记录保留新字段。现有二维码和native_share_code回归及JS语法通过，检查了长图裁片布局。测试未写入5208用户装配库，未重打包Windows。

## NUI-41：游戏/Pyfa EFT文本导入（待用户验收）

新增装配库及工作台入口、文本框/文件选择、角色选择、逐行解析与检查预览、保存新装配。解析服务/api/import/eft基于固定SDE精确名称映射，仅转换输入；所有业务计算/合法性由现有原生analyze完成。解析失败不部分导入；原生机制/条件问题允许明确保存草稿。支持范围、缺失信息的显式处理及网络核对来源见eft-text-import.md。

验证：4项解析/原生分析专项通过（英文、中英文名称、空槽、离线、弹药、库存、重复模块、错误行、数量边界、脑插/药剂、舰载机备用）；应用内浏览器5352隔离库验证粘贴→全技能V→校验→保存→装配库出现新记录，未知装备显示第2行且保存禁用；对话框视觉检查通过。未写入用户5208库。最初Playwright CLI验证被自动审批拦截，已改用内置浏览器公开控制接口完成验证，没有绕过该命令限制。接口本地r37不变，未改引擎/增加游戏机制，未重打包Windows。

## NUI-42：资源超限与原生准入口径一致（待用户验收）

发现适配器isValid只检查native.errors，忽略引擎为了保留草稿诊断数值而放在warnings中的RESOURCE_EXCEEDED。原生电容/药剂准入实际均把该警告视为阻断，导致UI“合法”而电容拒绝的矛盾。

现将原生RESOURCE_EXCEEDED原样加入UI issues（含details资源ID、used、limit、依赖和路径），isValid据此返回false；不重算资源、不修改引擎告警等级、不把其他非阻断警告一律升级。保留原始native对象及可用属性/输出。详情列表按完整文字去重，避免同一原生警告在issues和warnings中重复展示。工作台、EFT/图片导入、分享图和外部能量来源校验都消费同一isValid。

验证：2项准入专项+4项EFT回归通过。全技能V裂谷装中型四联轻型集束激光器：引擎errors为空，但RESOURCE_EXCEEDED明确72.9>51.25，UI判为false，诊断DPS仍有值，电容返回CAP_SCENARIO_FIT；CLI/MCP资源和警告相等。正常装配isValid=true且电容available，缺技能isValid=false。5352隔离浏览器EFT预览显示可导入草稿与powergrid超限，不再声称校验通过。仅UI适配/展示变化，引擎及本地r37契约未变；未重打包Windows。

## NUI-43：同口径估价与来源（待用户验收）

验收入口：装配工作台底部估价、装配库估价、生成图片。可展开分项与缺项；未声明弹量显示已知部分，声明后更新完整估价。真实零报价与缺报价区分，深渊实例不使用基底价格。ESI均价附时间、缓存状态和快照哈希，不承诺即时成交价格。

实现及验证详见valuation.md。5352隔离库实际装弹修改和分享生成通过；3项Python测试包含6种MCP/CLI完整结果对照，4项引擎估价与3项契约测试通过，JS显示边界与57模块检查通过。未重打包Windows，等待用户逐项验收。

## NUI-44：原生编辑事务（进行中，暂不验收按钮）

已完成独立原生事务通道及MCP/CLI同会话回归。UI预览/撤销/保存仍使用现有流程，尚未切换；须先解决静态覆盖不完整草稿的保存边界和UI元数据协调。基础验证详见joint-development.md的NUI-44a。

NUI-44b进度：原生诊断草稿策略及公开MCP/CLI验证完成。已解决会话无法保存带安装错误/缺机制诊断草稿的边界；错误和空值保留，不改变游戏准入。当前界面按钮仍未切换，暂无新用户验收操作。

NUI-44c待验收变化：保存期间换装配不会把旧回执写到新装配；保存期间修改脑插/库存仍显示当前改动待保存；撤销保持未声明货舱与空货舱不同。原生命令转换已完成无头验证，但界面尚未由原生会话接管。5352隔离库保存及120→0→撤销→重做页面验证通过。

NUI-44d进度：原生预览不再把缺失资源当0，基准/候选诊断与完整性均公开；r40接入通道验证通过。没有新增待点击的UI控件，完整事务按钮接入仍进行中。

### NUI-44e 保存入口（待用户验收）

“保存装配”现在先完成原生事务并核对导出快照，成功后才写装配库。缺技能/安装诊断的可分析草稿仍允许保存，不冒充合法装配；结构错误与版本冲突明确失败。保存期间切换装配仍不会串写回执。相同内容的失败保存可重试，同请求不重复创建。

5352隔离页面保存/重开/库存修改后再保存通过，实际原生快照与库库存均1002；3项真实持久化故障/重放/CLI对照和JS竞态回归通过。尚未接管实时预览与撤销控制器，详见joint-development.md NUI-44e。

## NUI-45：原生命令换装预览（待用户验收）

入口：选槽位后悬停装备浏览器物品，或拖到槽位预览。预览数值由引擎用安装命令生成，移开恢复、真正安装后应一致。没有临时装配入库；缺条件的候选保留原因。5352隔离页面200mm→150mm炮验证预览DPS45.61，安装后45.61，撤销恢复；CLI/MCP及引擎回归见joint-development.md。实际历史会话接管与Windows打包仍待完成。

## NUI-46：原生撤销、重做（待用户验收）

验收：修改装备/弹量/脑插或驾驶员，保存后点击撤销、重做；名称/标签也可撤销。提交中显示状态并暂时禁止重复编辑，网络结果不确定时出现“重试编辑”；保存结果不确定时再次保存恢复。打开另一装配不会把旧编辑回执应用过去，重新打开时历史仍按原行为清空。

真实页面0→10弹量、保存、原生撤销到0（保存快照仍10）、重做到10；改名/撤销及刷新再保存通过。控制器已用真实引擎验证create/apply/save丢响应重放，其他专项见joint-development.md。Windows尚未重打包，完整装配覆盖审计尚未结束。

## NUI-47：Windows构建（待用户验收）

已生成包含当前独立N引擎与CLI的Windows安装包：output/windows-r41-20260918/EVE-FitLab-0.1.2-beta.1-Windows-x64-Setup.exe。版本0.190.8-ui.1/r41。独立数据目录启动同批桌面程序，预览→安装→保存→撤销→重做→导出及包内CLI对照通过，桌面截图检查通过。

自动验证记录在output/windows-r41-smoke-001；没有运行安装/卸载流程，没有写入用户装配库，没有对外发布。整体面板与所有装配组合仍未全部验收，不能把桌面冒烟测试当成全量认证。

## NUI-48：刷新性能（待验收）

在现有赫卡特装配刷新后，检查布局保持、DPS 与槽位数值正常；重复刷新不应每次重新计算同一输入及下载完整装备目录。实测热刷新约 0.8 秒；服务重启冷刷新仍约 6.9 秒，尚未完全解决。修改技能、装备或情景仍应获得新结果。详见 performance-20260919.md；契约仍 r41，未改引擎，未重新打包 Windows。

## NUI-49：第一阶段状态与分享来源（待验收）

工作台摘要现在分别说明装配是否合法、当前结果是否完整；EFT/图片导入和分享图使用同一文字。未知属性不应出现 0 km/0 m，未知资源不应绘制零值进度条或误报超限。位置和布局保持原样。

新生成图片的装配码含引擎/接口/规则/SDE/索引绑定；其他版本来源在分析、详情、估价、预览或保存时明确拒绝，保留原文件，不静默重算。完整分享上下文和原生身份还待收口，不把此项当全链路无损恢复已完成。

发布队列及证据见 phase1-release.md；统一检查 84 项 Python、10 个 JS 专项与 61 模块检查通过。没有新安装包；正式发布尚未完成。

## NUI-51：完整装配文件（待验收）

装配库条目右键“导出完整装配文件”；底部空白右键“导入完整装配文件”。选择.fitlab.json后先验证来源和原生重放，再创建新装配。保留备注、技能、实例、库存、情景引用快照及历史报价；主记录本地ID不同而原生身份/hash不变。诊断草稿仍如实显示不合法/不完整。

冻结情景和报价会在工作台说明，不自动改为本地其他装配或当前价格。图片分享仍是原无情景图片，完整文件承担带上下文的交换。能力/验证及未关闭边界见fit-package.md；没有新安装包，入口及最终交互仍等用户重构设计。

## NUI-52：整合引擎隔离验收

新引擎工作区为http://127.0.0.1:56432/#library，当前5208未切换。可打开“整合验收33400-active”检查堡垒资源、电容容量和缺项、零速起步不适用；“整合验收483-active-slot7”应显示非法装配并保留CPU/PG。详情及测试记录见integration-r43-acceptance.md。没有导入旧会话，也没有发布。

## NUI-53：分类、弹药与晶体（待验收）

官员装备应在舰船装备的对应功能分支；新装普通弹药默认满仓显示如160/160，晶体默认100%。数值无按钮边框，点击变输入框，Enter/失焦提交、Esc取消。晶体编辑用剩余耐久百分比，50%即半耐久，备用晶体对话框同口径。检查保存/刷新/撤销后数值；原始未知库存的完整文件不擅自默认满仓。详见joint-development NUI-53。

## NUI-54：电容稳定性入口

电容标题直接给稳定/不稳定/暂无法确定；窗口和周期信息收进“电容详情”。无可用曲线时隐藏窗口选择。堡垒缺耗电数据时显示具体装备，不能显示稳定。当前引擎没有平均模型的预计耗尽时间，暂不展示虚构续航。刷新56432验收，最终布局仍由用户决定。

## NUI-55：旧版风格右栏与悬浮面板（待验收）

在56432工作台检查电容第一、攻击/防御标题内切换、紧凑分层抗性、总HP/EHP、质量/惯性、无人机带宽、感应强度。悬停护盾或速度等数值，停留锁定后继续查看技能/装备的修改前后与叠加说明；抗性可展开共振来源，DPS仍有引擎曲线和已选分项。

输出口径选择已收进“输出详情”。缺失/部分结果仍明确显示。220px栏宽、护盾技能二级面板已实际核对。该轮恢复结构与已有指标，不代表含换弹总DPS、齐射/伤害占比、维修汇总、预计电容续航都已对齐；详见joint-development NUI-55。旧版5207保留作对照，等待用户验收。

## NUI-56：电容旧版交互恢复（待验收）

电容主栏不再放窗口下拉、展开曲线及大段说明。标题显示稳定百分比或原生首次供电不足时间；悬停标题查看电量曲线、移动探针及计算条件。正文恢复回充时间与模块耗电行；模块耗电可查看单项来源。当前帕拉丁实际查得10分52.5秒后首次供电不足。没有终点/缺项的装配不得伪造耗尽时间，详见joint-development NUI-56。

## NUI-57：整栏回归旧版设计（待验收）

对照5207与56432：攻击四行及四类伤害条、防御维修行、机动/锁定顺序、无人机和感应系统；新版自行增加的输出/口径/诊断/估价折叠入口应已消失。来源与缺项从数值、总DPS、装配状态和估价悬浮查看。当前右栏已调回对照页288px宽。

未接通的原有指标保持原位置—；含换弹与四类伤害比例、维修合计/EHP折算等尚待数据接通，不应误验为数值完整。新交互先经用户确认，详见joint-development NUI-57。

## NUI-58：伤害和维修汇总接通（待验收）

旧位置上的四类伤害数值/条形比例、普通弹仓含换弹输出、本舰维修合计及HP/EHP折算读取r44原生inspector。检查切换来伤、启停多个维修器、部署/收回无人机后的结果；无维修时应为0，缺机制时应为—而非0。智能炸弹应计入武器输出与齐射。

当前势力晶体换弹结果仍可能为—，悬浮显示真实原生原因；有限次数舰载机副武器不冒充无限续射。整体布局和入口无新增。引擎/接口/验证见joint-development NUI-58，升级前核对UI-LOCAL-009补丁。

## NUI-59：返回装配库响应（待验收）

点击“← 装配库”应立即看到本地已有库列表与当前草稿，保存/权威刷新在后台进行。正在保存的那条会暂不可打开，结束后恢复。检查保存失败仍有草稿、快速切换其他装配不会被旧回执覆盖；布局不变。

## NUI-79：舰船详情资料（待验收）

默认进入舰船特性，分别显示技能每级收益和船体特有加成；所需技能页显示直接前置及罗马数字等级。核对裂谷两条技能收益和神使船体特性，原背景描述及基础/装配属性仍可查看。不显示未经核验的角色满足或递归技能链状态。

## NUI-78：空白右键新建（待验收）

菜单顺序为导入、新建装配、粘贴。选具体船直接出现名称表单；选分类则先出现舰船树，滚动到该分类、只展开一级（必要祖先可见，后代不递归展开）。选择船后下一步进入原表单；关闭选择器不改变主树展开偏好。

## NUI-77：能力发现替换（待验收）

在56454检查赫卡特模式候选、T3空草稿子系统、被动/主动模块菜单、晶体/普通弹药/脚本兼容浏览，以及不同原生无人机上限。已改为公开能力查询，首次查询期间状态不应误判为没有能力，安装前仍由原生规则校验。布局沿用原界面。

## NUI-76：r54独立验收环境（待验收）

http://127.0.0.1:56454/#library；旧库未删除，新环境为空库。检查管内待命仍占分类配额、备用不占管、鱼雷可选、炸弹按钮可见且正确保留政策提示；舰载机和无人机能力独立显示。完整进度见integration-r54-acceptance.md。

## NUI-72：指挥航母（待验收）

装配库在舰船→旗舰→指挥航母找到四种新舰船；进入装配后显示原生舰载机发射管。基础4管，轻型2、支援4、重型0；实际显示仍以技能/装备后的原生结果为准。不要显示虚构普通无人机带宽。

## NUI-69：空装配零曲线（待验收）

替代NUI-68的文字空态：无攻击来源时，三个页签均绘制0水平线，显示没有攻击来源及0DPS/EDPS。有装备但计算不完整时不能画零线。

## NUI-68：无攻击来源的悬浮曲线（待验收）

空装配悬停总DPS应显示0，并说明当前没有攻击来源、暂无攻击曲线，不应再报“所选输出不完整”。装入攻击来源后仍使用引擎实际曲线，缺失贡献仍显示原生诊断。

## NUI-67：空装配攻击数值（待验收）

没有攻击来源时，总DPS、武器DPS、无人机DPS与齐射显示0；四类伤害空条显示0%。伤害比例悬停仍说明总输出为零、比例无定义。缺失、未支持及静态阻断不得因该改动显示为0。

## NUI-66：舰载机安装失败后管位交互（待验收）

夜神装入修道士II应显示支援中队超过上限1/0；失败后不得留下拖放虚线，仍能选其他管位。真实选择只高亮一个管位，重复点击或Esc可取消。检查拖放成功/失败/取消后的高亮清理和再次操作。

## NUI-65：装配库舰船右键（待验收）

左侧舰船浏览器右键具体舰船，鼠标附近出现新建装配、舰船详情。未左键选中也应对右键舰船操作；新建预填正确船名，详情显示该型号基础资料，不混入当前装配属性。支持键盘菜单键/Shift+F10。

## NUI-64：舰载机市场树归属（待验收）

覆盖NUI-63的顶层入口设计：从无人机→铁骑舰载机→航母铁骑舰载机进入轻型/后勤/重型分类，再进入科技/势力子类。各市场分类使用原始市场图标。无需选发射管即可浏览，搜索结果保留同样层级。

## NUI-63：标准舰载机浏览分类（待验收）

不选发射管也能在装备树找到铁骑舰载机；轻型、支援、重型下继续按一级科技、二级科技、势力分类。检查普通搜索、展开收起、发射管筛选/清除筛选、右键和拖动安装；无发射管舰船只浏览，不提供可执行的安装项。

## NUI-62：舰载机附加武器默认开启（待验收）

新装入中队时，可计算的重型火箭齐射等附加武器默认计入，有限弹量武器自动切换到对应周期口径。手动关闭后，调整数量、参战状态或移动中队不应重新开启；备用只预选，部署后才计入。已有中队的手动选择不改写。

## NUI-61：舰载机分类配额（待验收）

轻型、支援、重型显示已部署中队数/上限，例如1/4；未满绿色、已满黄色、超限红色，与炮塔/导弹挂点一致。待命和备用不计入，悬停可查看说明。没有原生计数时显示—，不冒充0。

## NUI-60：舰载机火箭计入输出（待验收）

右键中队，火箭按钮应显示“点击计入 · 有限弹量”，不再要求前往已删除的口径下拉。点击后计算口径自动切换并计入；最后一个有限次数武器取消后恢复名义口径。检查多中队、待命、撤销/重做及保存后的选择与口径一致；持续输出不应因这个选择被虚构。

## NUI-80：装配库新建与导入入口（待验收）

- 列表不再显示新建按钮，右键仍可新建；页头只保留扫描码样式的图片导入图标。
- 新建装配窗口右下角提供文本导入、图片导入，点击后切入对应导入流程。
- 标签以标签块展示，可输入添加、移除和选择已有标签；重复输入不重复添加，Enter 不误提交装配。
- 装配库舰船树和新建舰船选择器显示四大帝国势力标志。

已验证三个导入入口、标签增删去重、势力图片加载和 1045×912 布局；未变更引擎接口。视觉与交互等待用户验收。

### NUI-80 修订：复用装配页的加号标签交互

按用户反馈取消新建窗口常驻输入框、标签删除叉号及建议区。新建与装配页共用同一标签组件：默认显示＋，点击后就地输入；点击已有标签编辑，Enter/失焦确认，Esc取消，清空后确认删除。样式沿用装配页原有 fit-tag/add-fit-tag/fit-tag-input。已验证默认无输入框、添加、编辑取消、清空删除；68模块检查通过。等待用户验收。

NUI-80 尺寸调整：新建窗口标签区域最小高度34px，加号22px、点击区域至少34×34px；标签及编辑文字13px。限定于新建窗口。待视觉验收。

## NUI-81：等级对应技能点（待验收）

角色列表、技能页和装配驾驶员选择器应显示0 SP或引擎计算的等级最低SP，全V为641,792,000 SP（当前511技能）。修改技能等级应更新；未知值保持—并可悬停查看原因。不包含真实账号额外训练进度、未分配SP。引擎新增能力由用户本次特许，已分别登记本地补丁UI-LOCAL-010。

## NUI-82：官网角色授权（待验收）

在当前角色管理＋→从EVE官网导入，本人完成官网登录/验证码/授权后应回到56454并选中新角色；显示头像、活动技能及官网实际SP。重复授权更新原记录，不新增重复角色。取消或失败显示原因且不写入；官网SP悬停可见来源时间、未分配SP。复制为自定义角色后使用等级最低SP。

自动链路专项和原生CLI/MCP快照验证通过，真实官网要求真人验证码，因此真实账号回调尚待用户完成。5207现在由当前服务用于回调，旧对照服务已暂停、文件数据保留。

## NUI-83：方案库新建入口（待验收）

空态显示“大加号＋创建新方案”整块槽位，悬停/键盘聚焦有边框、背景和加号变化；方案库右上角＋始终新建最外层方案，进入原有命名流程。检查原有未保存确认仍生效。仅UI调整。

## NUI-84：方案加成（待验收）

编辑区右上角显示当前脑插/增效剂方案词条，收益/负面区分，悬停查看来源与条件，多项内部滚动。安装卸下、计算角色、手动副作用和随机服药后更新。不同武器适用范围分列，不伪造统一舰船数值；套装内部倍率不重复列出。

已验证高级蝰蛇完整套装速度+24.738%，卸Omega降为+7.770%；蓝丸已启用护盾副作用-20%，关闭后消失。空方案无词条，未知效果明确提示。引擎LOCAL-012，等待用户视觉/交互验收。

## NUI-85：可拖动的方案加成悬浮球（待验收）

加成面板脱离工具栏，默认收为52px悬浮球，点击展开、拖动移动、位置与状态记忆。Esc或收起按钮关闭；切离方案页隐藏，窗口缩小不超出视口。顶部工具栏压缩，扩大脑插与药剂编辑区。已验证点击、拖动、键盘、刷新及切页行为；引擎计算不变。

NUI-85追加验收：浮层四边/四角可拉伸，刷新保留尺寸；右上角图标恢复默认大小且不收起。球仍负责展开/收起，缩放窗口不应让面板越界。

## NUI-86：方案文字与图片分享（待验收）

方案更多菜单→分享文字/分享图片；方案库→导入。文字可复制或保存，PNG可保存或复制；导入文字/图片后核对名称，再保存为最外层新方案。检查脑插、药剂、副作用和技能快照还原，原方案不被覆盖；多张二维码必须完整。版本不符或损坏应明确拒绝。自定义FITLAB-PLAN/1格式，图片数字来自原生词条汇总。

## NUI-87：激光晶体装填修复（待验收）

幽灵级三门重型脉冲激光器II可批量装填/换弹，不再因候选晶体库存提前改变当前装配而报FIT_INVENTORY_CRYSTAL_MOUNT。检查撤销/重做、旧晶体ID和磨损保留。刷新旧错误草稿时将不匹配晶体保留为货舱备用并重新计算。仅UI快照与身份一致性修复。

## NUI-88：安装/移除/预览性能（待用户验收）

不更换布局。悬停与安装共用完整候选分析、晶体身份；预览去重排队；提交期间允许浏览；HTTP压缩和简要事务回执。独立引擎LOCAL-013。请验收连续悬停、单件/批量安装、移除、撤销/重做及切换槽位。证据和剩余延迟见[性能记录](performance-20260920.md)。不宣称首次所有装配瞬时响应。

## NUI-89：首启装配库阻塞修复（待验收）

舰载机目录独立异步加载，复用固定SDE元数据，取消约9秒的冷目录查询。rc.2包内首启及编辑回归通过，来源/指标契约不变。详见startup-rc2.md。

## NUI-90：深渊手动编辑、期望次数与实际试验（待验收）

可编辑范围内数值并保存安装；默认全部选中属性达到编辑值或更好，也可指定区间。概率和实际试验由r58公开引擎计算。支持首命中停止、手动停止/继续，手动来源与随机凭据区分。详见abyssal-edit-and-trials.md；本轮未打包。

### NUI-90 交互调整：正常／编辑模式

按用户标注，标题旁增加模式切换，默认正常。属性结果改为纯文字，编辑模式直接拖动原范围条，正常模式及请求进行中锁定。拖动实时更新数值/亮点，允许范围内1000等分；键盘方向键微调，Shift加大步长，Home/End到边界。切换模式不丢弃当前值或合格条件，应用/保存仍由引擎校验。浏览器验证只读锁定、拖动、端点、切回锁定和引擎校验；72模块检查通过。引擎未改，未打包。

### NUI-90 随机／编辑分工（最新交互，以此为准）

“正常”改为“随机”。随机模式使用双滑块设置合格上下限，绿色填充；固定红刻度为编辑值参考，不重复写数字、不能拖动。白点及右侧数值为独立随机候选。勾选属性须全部落在区间内，初始为完整允许范围。随机一次和连续试验都不覆盖编辑值，只有“应用结果”才写入编辑模式。编辑模式只编辑和保存装备数值。切换模式保留区间、编辑值与候选；编辑值变化不重置独立的随机试验条件/进度。

浏览器验证了随机一次/连续试验与编辑值隔离、应用结果后覆盖、双滑块和红线、区间保留及概率查询；72模块检查通过。截图output/abyssal-dual-mode.png。引擎接口未改、未打包。

### 舰载机T2角标修复

舰载机图标复用本地T2左上角素材，根据metaGroupId=2显示，不依赖名称。装备浏览器和工作区/备用机库共用fighterIcon。浏览器对照阿米特I无角标、II有角标，列表图标保持30px，72模块检查通过。未改引擎、未打包。

### 浏览器定位交互统一

舰载机详情左上角采用真实装备浏览器分类路径，并与普通装备共用locateItem：清除搜索/筛选、展开路径、滚动高亮。舰载机右键定位不再切换发射管选择或按名称搜索。普通装备/弹药菜单及无人机/货舱菜单补充“在浏览器中定位”；舰载机菜单统一经公共菜单入口调用。深渊模式下定位会回到装备浏览器。浏览器验证舰载机详情标题、右键定位和普通装备定位结果一致；72模块检查通过。无引擎改动、未打包。

### 突变道具切换卡死修复

规则清空时旧滑条仍存在，controls访问null规则抛错且busy未复位。规则重置移入受保护的action，清空旧视图再查询；控件状态容许暂无规则，异常统一finally解锁。浏览器验证连续切换三种材料并随机、编辑模式切换后应用、模拟503后可关闭/重选并恢复，无pageerror。72模块检查通过，未打包。

### 深渊范围条增减颜色与数值标签

变化百分比按引擎comparison.changeState显示增益绿/减益红；参考刻度到候选结果间的区段依源highIsGood呈现方向，未知保持中性。百分比基准是原装备，随机区段基准是编辑参考值。编辑刻度、随机结果及双滑块上方显示展示单位数值，靠近时错层、边缘内收；拖动/键盘微调/窗口调整更新标签。浏览器确认耗电增加红色、负值属性改善绿色、四种标记标签及实时更新；72模块检查通过。截图output/abyssal-colors-labels.png。引擎接口未改、未打包。
