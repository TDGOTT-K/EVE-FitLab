# 装配工作台性能改造 · 2026-09-20

此前完整分析、预览、事务和展示之间重复构图、求值与传输；全技能装配携带大量未显示的技能追溯。后置电容仍占用与装配相同的串行 MCP 通道，前端整体替换槽位与属性 DOM。减少防抖无法消除这些成本。

本轮接入 0.211.0-ui.workbench1 / r59 / 38 工具。公开 fit_workbench 将首次建会话、编辑、安装约束和展示投影合为一次原子请求；事务从不缓存，丢回执使用相同 requestId 重试。普通装卸不再额外发完整分析；自动补弹等候选准备可能仍需独立查询。

引擎复用来源隔离的类型模板及最多8项、估算32 MiB的编译图快照；紧凑求值只展开非技能根与实际依赖，未知/不完整静态机制回退原路径。完整 fit_analyze 保留。输出选择继续使用原生 reducer，前端没有新公式。

主回执保留非技能属性值、错误、unsupported及完整性，明确声明省略追溯和成功技能覆盖；悬停时通过 fit_attributes 读取完整追溯并核对 fitHash。DOM仅更新变化节点。电容、曲线及追溯使用第二个只读MCP进程隔离排队，代价是额外常驻进程、SDE和缓存内存；只读通道拒绝编辑请求。

## 最终本机测量

Edge无头，1045×912，真实金鹏保存快照（24槽、511技能、脑插/增效剂、显式情景目标）。仅操作隔离浏览器草稿及编辑会话，未保存用户装配库。计时从页面 calculation-invalidated 到右侧 engine-status 完成，包含请求、适配及同步DOM更新，不含打开菜单时间、后置电容完成时间，也不等于屏幕扫描输出延迟。

- 新会话首次卸载：316.1ms。引擎进程和SDE已热，不能称冷启动。
- 20次撤销/重做：中位约125ms，P95（nearest rank）143.8ms，范围112.3–246.9ms。重复状态可命中缓存。
- 12个连续新状态卸载：中位约112ms，范围99.1–270.9ms。后面的装配逐步变简单，不能代表所有重装配。
- 先前同金鹏端到端样本约872–1016ms；不是严格同期统计A/B。
- 原生分析JSON示例由2,163,950减为455,772字节，约减少79%。这是原生未压缩分析，不是整个HTTP回执。
- 连续编辑后未修改中槽保持同一DOM对象。按需PG追溯显示113→101.7→76.275MW，悬停锁定正常，电容后置完成。

原始计时 output/workbench-ui-perf-final.json、截图 output/workbench-ui-final.png 留本机；不提交用户装配输入。性能缺口保持开放：偶发250–271ms尖峰、冷启动、不同重装配和持续后台负载仍需跟踪。未宣称所有操作P95小于200ms。

## 验证

新增原生工作台3项（完整值/输出对照、事务拒绝/重试/撤销重做、缓存淘汰与调用者隔离）、3项冻结契约通过；另已通过37项Dogma、112项突变/状态/弹药/热量、9项电容、4项曲线、4项分析缓存专项。

Python最终7项覆盖实际MCP/CLI工作台读写一致、只读拒写、读缓存隔离、预览和导弹曲线。Node原子编辑历史覆盖单请求、丢回执重试、撤销重做、技能排序及UI-only no-op；81个浏览器模块检查通过。首次Node测试未绑定当前引擎路径，命中旧副本报告Unknown tool；设置FITLAB_NENGINE_ROOT后通过，未修改断言。

未打包、未推送。并行本地化修改保留工作区，不纳入本轮提交。
