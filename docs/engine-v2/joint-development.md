# 联合开发：无头口径对齐与上游交接

状态：约定已登记，现有界面全量无头口径对齐尚未完成。现用引擎 0.190.0 / nengine-headless-v1 r33 / static Dogma v54 / output-contributions-v3，30工具，SDE3503375。本批只登记开发约定与审计，不改运行时接口版本。

## 独立补丁基线

- 引擎唯一写入路径：D:/AI/GPT6/N号引擎-UI接入-0.190-r33。
- 交付源：0efa261a7f0d1a5b53481a5d6798a1007faff980；独立副本交付：fe38afaf57b9faec7e5f0a811545068ff47df86c。
- UI 接入基线：4339a4a。
- 引擎补丁清单：该副本 docs/ui-local-changes.md。UI继续独立提交，不能把两个仓库混成一个交付。
- 原 UI-BASELINE.json 是历史交付证据，不能把本地新构建伪装为原交付；真正修改契约时另登记派生构建、契约版本、哈希，并调整 bridge 的验证来源。旧冻结契约不可覆写。

## 首轮审计及优先顺序

| 优先级 | 展示项 / 当前入口 | 无头现状 | 要补的结果和上下文 |
| --- | --- | --- | --- |
| P0 | nengine_output.py 分组小计、选定总量 | fit_analyze / sde-fit --metrics 已有 selection reducer；UI展示聚合允许，但须用对应选集复现 | 明确同 metric/unit/aggregationKey，保存选集，未知/多组/空集状态不得丢失 |
| P0 | nengine-view.js / scenario-display.js 基准比较；dps-chart.js 百分比 | 当前值/基准可分别查；差量、比例尚非公开输出字段 | 同输入和明确基准的绝对差、相对变化、应用比例；零基准/负基准/部分数据须有定义与原因，不人为夹到100% |
| P0 | nengine_curves.py 理想参考、取样网格、采样峰值 | 具体 targets 的值已有 MCP/CLI；参考目标政策与自动采样方案仅在宿主 | 版本化参考政策S/M/L/XL、混装规则、轴/范围/点序列/当前点、所选口径/峰值定义，CLI/MCP可完整复现；峰值注明采样峰值而非解析最大值 |
| P0 | abyssal-workbench.js 改善计数、差量、区间位置 | mutation_rule/roll已有真实原值、绝对变异值和回执；页面还独自算派生比较 | 变异前后比较、好坏方向来源、零/负基数语义、未知方向与范围外原值；保留完整精度与历史回执 |
| P0 | booster-roll.js 随机服用及基础概率 | r33已有独立 booster_plan_analyze/roll/verify 和对应CLI；UI尚用本地随机 | 改用公开随机回执，技能/脑插上下文、坏分支及先决条件分别显示，无船体方案不伪造舰船 |
| P1 | 情景固定层 EDPS | context.output.target/targets + effective指标已有 MCP/CLI；UI未接 | 原始目标装配/解析目标层、穿透比例来源、输出集合、基准及差量/比例；不将层推进混入静态期望 |
| P1 | 本舰传电/毁电、电容续航/曲线 | capacitor_scenario 和 sde-capacitor-scenario已有；UI未接 | 绑定外部sourceFit/moduleId、距离、时间政策、样本、有限共享供应；平均完整性/周期付款失败/窗口结束分离 |
| P1 | market_prices.py / app.js 装配估价、材料价格 | ESI来源+HTTP /api/prices；未有公开MCP/CLI同口径估价出口 | 独立外部数据宿主或引擎输入快照接口（设计待定）：typeID、来源URL、获取/供应时间、币种、缓存与缺报价，估价数量及缺项同口径输出 |
| P1 | 总技能点及角色/方案摘要 | 历史UI有占位；须继续逐项核查生成入口 | 真实技能等级、技能rank或ESI训练快照及时间来源；mock不得冒充真实SP，缺数据不填0 |
| P2 | 物品来源说明、价格外其他外部数据、图片 | 固定 metadata工具已有；图片/市场树为外部资源，未形成全量版本化包 | 固定数据来源及版本、资源映射清单；图标无需游戏公式，但来源身份应可无头查询 |

以上是首轮定位，不是穷尽全应用业务数值的合规认证。后续每个面板继续核对估价、修量/续航、百分比、随机结果和默认假设，不能把已有前端公式遗漏在最终验收之外。纯单位换算（如角速度×10km得到横向速度）、格式化和布局可以留在UI，但所用10km参考必须随上下文公开。

## 实施门禁

1. 先定义输入、结果字段及不可用语义，再在引擎共享实现，MCP/CLI用同一路径；不先造UI独有算法再补接口。
2. 使用当前副本专项验证，记录实际命令/输出；禁止将交付方测试记录当成本地新增功能测试。
3. 同输入调用MCP和CLI，比较数值、来源、selected/notSelected/exclusions及上下文；验证零/空值/负值/不兼容口径/未知能力。
4. UI只展示规范结果或契约允许且可无头复现的聚合，去掉被替代的本地业务计算入口。
5. 分别提交引擎与UI，关联两端commit和用户验收项。不得宣称仅文档登记等于接口实现。

## 下一批

先处理P0中的输出比较（基准差量、比例）与曲线查询政策，共用公开分析契约，并给突变结果比较提供相同的明确数值语义；随后接EDPS和电容情景。具体版本号须在实现时与当前契约生成/冻结流程核对，不提前伪造新revision。

## 升级检查

收到新副本：先比较 ui-local-changes.md 中的每个补丁与新上游提交，记录已合入/需移植/已替代/冲突。未完成该检查不得仅切默认路径或覆盖本地副本。历史存档从原始输入重新校验，原回执和绑定保持原样。上游通知只准备清单，不擅自向其他任务发送消息。

## 约定登记提交

引擎文档提交：fbf4851（仅当前r33独立副本）。UI约定与审计提交见本文件Git历史。两个仓库运行时代码未在本批改变，接口仍为r33；业务无头对齐缺口仍按上表待办。本批检查文档路径与diff，不把先前运行时测试冒充本批新接口验证。

## 2026-09-18 联合实现批次 LOCAL-001a

引擎提交97f59d7；本地派生0.190.1-ui.1 / r34，非上游r34。原交付UI-BASELINE与r33冻结产物保留，UI显式读取当前副本UI-LOCAL-BASELINE并验证实际discovery，使用新nengine-ui-local-r34状态目录。打包准备包含两份来源清单。

已完成：同一分项/所选集合及每个目标采样的current/baseline/delta/ratio/relativeChange公开输出；现有MCP和CLI可直接获得。部分结果和零分母保持不可用原因，覆盖隔离后再生成比较。UI原生曲线改用引擎ratio，仅做百分比单位换算；悬停说明消费引擎delta。

证据：引擎输出15项+公开契约3项通过；3类真实CLI/MCP、Schema与独立算术对照通过。UI侧静态/分项/情景17项通过，新增当前采样比例同原生比较的一致性断言通过。独立5340浏览器实际显示0.61 DPS、基准10.56、差量-9.95、比例约5.75%，曲线显示同一引擎比例。未重复全量、未做战斗。

未完成：自动参考/采样政策和采样峰值无头入口、突变比较、跨装配换装差量、独立药剂随机、外部数据与估价、其他静态面板接入及全装配覆盖仍按原目标推进。本次不代表全应用符合无头口径要求。下一批继续曲线政策与EDPS，不缩小目标。

## 用户范围补充与 NUI-22

用户明确：缺失游戏机制不开发，仅消费引擎已支持能力；联合修改限公开接口、查询组合、上下文、诊断与UI展示。已同步两仓库AGENTS。目标的“正确显示”包括支持范围内的真实结果，以及不支持项明确状态/原因，不承诺补齐引擎缺失机制。

NUI-22已接原有固定层EDPS，DPS/EDPS、目标层来源、分类/装备行和曲线一致；新增实际CLI/MCP对照通过，详见验收台账。自动曲线政策/峰值无头出口、突变比较、药剂/库存/电容/估价等原待办继续保留，未因当前接入成功删除。

## NUI-23 已有药剂方案接口接入

本地JS抽签删除，改用已有独立booster_plan_analyze/roll/verify，提供角色技能快照、脑插上下文和可重放回执；副作用概率/时长来自引擎。原生CLI/MCP一致性、篡改/保存与浏览器验证通过，详见验收台账。仅新增FitLab静态白名单与HTTP映射，不改引擎版本或机制。P0药剂独有随机入口已消除；其他未完成项目仍保留。

## NUI-24 普通库存接入

已接原生普通cargo和magazines，移除装备行本地弹夹容量公式展示；数量/容量/体积/超限来自原生分析并有实际CLI/MCP对照。晶体实体编辑及原生电容情景仍未完成。市场估价仍有独立满弹假设，需在外部数据无头出口批次统一，不能把此次库存接入视作估价口径已完成。

## NUI-25 电容已有接口接入

本舰周期情景、画布外部传电/毁电来源绑定、有限库存及平均完整性已接入；记录查询政策/来源并实际CLI/MCP对照。平均稳定与付款失败、供给停止和窗口结束分别展示。不新增游戏机制。NOS固定对方电容上下文UI仍未接，明确返回缺条件，不视为引擎缺机制。

## NUI-26 实体晶体接入

接入已有晶体身份/损伤/位置投影，新增编辑及备用装载、卸载保留、交换与撤销保存。CLI/MCP库存一致，6项Python及JS/实际浏览器通过；没有开发磨损机制或UI公式。历史普通晶体堆叠仍拒绝而不推测损伤，迁移交互待做。另核实UI旧目录SDE3248221与引擎3503375不一致，server.validate_fit对新技能存在误拦截，应纳入原生目录/校验迁移。

## NUI-27 当前目录全集

已消除refresh_catalog以历史ID白名单过滤当前副本的问题：从当前索引生成完整装配目录，全技能V含511项。新船/新技能通过宿主及原生校验，目录集合与分页MCP核对，新增类型元数据CLI/MCP一致。来源版本与历史导航分别声明，目录存在不作为支持认证。市场树独立资源、技能点无头口径、元数据全面展示等待办仍保留。

## NUI-28 技能点边界

当前引擎无技能点总量查询，SSO未存SP来源。删除UI独有SP公式，三处显示明确不可用；不按本任务新增缺失训练机制。该能力仍不具备，保留后续来源/引擎支持验收项，不能标成无头数值接口已完成。

## NUI-29 吸电固定边界

完成已有静态电容接口的NOS上下文接入：画布设置本舰起始电量和对方固定GJ，绑定目标容量/抗性及外部sourceFit，不计算对方稳定性。5项专项、两类CLI/MCP完整对照及两条浏览器保存流程通过。接口版本不变；曲线自动政策、突变比较、估价与其他全量接入待办继续保留。

## NUI-30 按需属性

现有fit_attributes已接物品详情，修复主分析属性子集漏项与弹药身份错误；原生轨迹、不可用/策略/覆盖状态和诊断保留。3项Python专项、JS空值语义与浏览器炮台/弹药验证通过，CLI/MCP一致。舰载机等其他上下文物品详情、统一结构化问题跳转仍待继续，不能用本批覆盖整个详情系统。

## NUI-31 舰载机详情上下文

发射管和备用机库具体中队身份已接fit_attributes，页面标明单架属性，沿用原生轨迹和状态。测试与浏览器确认两类中队48项查询身份正确；不影响DPS选集或部署。其他剩余清单继续保留。

## LOCAL-002 / NUI-32 突变比较

引擎3d41208提供公开comparison及comparisonSummary，升级本地0.190.2-ui.1/r35；UI消费派生输出，历史回执核验不改写。公开5案例、契约/变异专项、UI存储和浏览器验证通过；来源规则未知方向不以旧metadata回填。旧r33/r34契约保持，主工作区未动。自动曲线政策、估价、图片分享、事务与其他全量接入仍待继续，不将本批当成总目标完成。

## LOCAL-003 / NUI-33 公开曲线政策

独立引擎dc5a2f3+14d1841新增fit_output_curves/sde-output-curves，本地0.190.3-ui.1/r36。自动参考、网格、当前点、比例和采样峰值均从公开接口取得；UI保留单位换算和图形布局。7项UI专项、引擎/契约与8案例公开对照通过，图表切换/EDPS页面验证通过。缺武器尺寸时需要具体目标，未以默认值掩盖缺条件。估价外部数据、图片分享、原生事务等其余待办继续推进。

## NUI-34 被动装备状态适配修复

通用非默认 online 效果不再被识别为启动效果；修复目录中 971 个错误可启用标记及旧 Active 草稿/图片/保存状态。能力来源仍为当前固定 SDE，不以查询成功与否判断主动性。6 项 Python 回归、JS 图片兼容、MCP/CLI 完整分析对照与浏览器安装/保存通过。引擎无需补机制或放宽输入校验，接口保持本地 0.190.3-ui.1/r36。升级时保留 UI 的默认效果分类和旧状态迁移逻辑。

## 测试阶段范围更新（2026-09-18）
用户取消旧装备、旧配置和旧数据兼容要求。旧晶体堆叠迁移不再是接入待办，刚开始的未提交迁移代码已撤回。后续集中当前格式下的功能、接口和结果正确性，不新增历史数据兼容层。仍不开发缺失游戏机制，仍不单独设计仿真战斗沙盒。

## NUI-36 当前物品详情来源统一
脑插与增效剂基础详情改用固定引擎 SDE，附 provenance，四物品 MCP/CLI 一致及浏览器入口验证通过。仍需统一方案浏览器目录/悬停内容与已装方案项计算详情。引擎接口本地 r36 不变。

## NUI-37 当前方案目录
已完成脑插/增效剂运行时目录、槽位、悬停来源和副作用定义接入固定 SDE；分类策略仅用于导航。完整 ID 对照、来源追溯、概率条目与套装/浏览器检查通过。引擎接口不变，UI 接入记录见 NUI-37。

## NUI-38 独立方案项参数详情
已接入公开 booster_plan_analyze 的属性及依赖来源链，方案角色/副作用上下文随右键快照传入。未返回的脑插属性显式不可用，完整性失败不以基础值兜底。完整脑插属性枚举仍待接口接入；新增机制仍不在范围内。

## LOCAL-004 / NUI-39 脑插源属性枚举
引擎113d3e0升级本地0.190.4-ui.1/r37，仅扩展既有独立方案属性查询节点。纯脑插CLI/MCP一致及真实装配对照通过，UI直接消费公开trace；NUI-38的脑插枚举缺口已解决。其他估价来源、分享图、事务和全量接入仍待继续。

## NUI-40 分享输入协议（进行中）
已补EVF3当前原生装配输入完整往返与二维码PNG识别，nativeFit/native结果一致验证通过。新引擎分享暂不开放：尚须替换旧数值渲染、显示全部新配置与来源，然后做图片生成/扫描/导入端到端验收。

NUI-40续：原生长图和实际PNG导入保存已完成，报告结果往返一致，刷新装配库的next引用错误已修复。原生图片现已开放；估价、图片曲线和其他未接入项按记录继续，不宣称总目标完成。

## NUI-42 准入一致性
修复适配层遗漏RESOURCE_EXCEEDED阻断警告。校验状态沿用引擎电容/药剂准入既有规则，保留原始诊断数值，CLI/MCP及导入页面验证通过。

## 浏览器入口静态回归检查（2026-09-18）

新增 npm run check:ui：对56个浏览器业务模块做作用域绑定检查，覆盖读取、赋值、更新、循环绑定；明确浏览器与ECMAScript全局名，不依赖Node运行时全局对象。自检含先前extraFolders赋值和next读取两个真实故障类型。随后esbuild仅在内存检查app.js模块图，验证静态模块导入/导出，无产品构建产物或应用状态改动。

当前检查通过，没有发现新的未定义变量。该检查用于预防入口ReferenceError，不替代游戏机制、数值口径、异步交互或Windows打包验收，不据此宣称整体接入完成。第三方vendor和生成目录不纳入业务变量扫描。

## LOCAL-005 / NUI-43：公开装配估价（待用户验收）

工作台、装配库及分享图统一消费fit_valuation，不再由前端推定满弹仓和计算总价。引擎独立副本升级0.190.5-ui.1/r38，固定SDE及游戏规则不变；外部ESI快照保留来源并支持CLI导出，适配API返回精确公开查询输入。缺失数量/报价、深渊实例和过期状态均明确保留。接口、范围、验证及无头复现见valuation.md。

本批已完成公开估价接入，原生事务、Windows打包及全量覆盖等仍待继续；不涉及战斗沙盒。引擎/UI分别提交，升级副本前须保留LOCAL-005查询与来源契约。

LOCAL-005提交对应：引擎7b18749；UI 88291a3。用户验收项NUI-43，上游尚未合入。

## NUI-44a：原生事务通道（基础接入，UI尚未切换）

增加/api/native-session/create、import、inspect、preview、execute、export，只转发公开引擎对象；通过现有固定副本MCP持久化，宿主不直写引擎会话。NEngineError保留结构化code/details/issues；STALE_REVISION和REQUEST_CONFLICT转为HTTP409。无自动重试，无新请求ID替代失败事务，不开放战斗工具。

test_native_sessions在临时状态目录验证创建、只读预览、提交hash、进程重连后的同请求重放、版本冲突、请求冲突、撤销、CLI重做、保存、导出和导入；CLI/MCP预览/检查/导出/重放结果一致。非法高槽7候选preview不可提交，apply拒绝且原会话完全不变。Python语法检查通过。未向用户库写测试数据。

发现的集成边界：EveEditor.Create/Validate/Execute/Export要求静态覆盖完整，并拒绝非SKILL_REQUIRED错误；现有UI允许不完整草稿。尚不能直接替换本地history或/api/save，否则改变现有草稿能力。后续需明确持久化草稿与游戏准入的界限、分析条件与编辑分离、UI元数据存储和并发保存，完成后才切UI。此条不代表事务功能用户验收完成。引擎仍r38，无引擎源码改动，无新增机制。

## LOCAL-006 / NUI-44b：诊断草稿策略已接公开通道

独立副本升级0.190.6-ui.1/r39。create可显式allowIncompleteDraft=true，安装诊断和机制不完整结果可持久化；结构异常仍拒绝。保存成功不等于合法、可计算或可战斗。策略随导入导出及复制保留。UI通道允许新参数，宿主/Windows入口绑定local-r39；未开发缺失游戏机制。

验证：22项原生编辑专项、3项公开契约及UI3项会话专项通过；新增诊断草稿从MCP创建、CLI保存与导出、MCP导入完整对照，并验证CLI独立创建的状态与MCP完全一致。既有严格拒绝和重放测试保留。引擎变更登记LOCAL-006，等待上游合入。

下一步仍需接界面：UI状态与引擎会话的映射、分析条件不污染编辑历史、失败/重试及并发保存。当前仅底层通道，不将本地history或/api/save冒充已接原生事务。未跑全量/Windows打包。

LOCAL-006提交对应：引擎2129773；UI 7490e6a。

## NUI-44c：编辑命令映射与保存竞态

新增nengine_edit_commands.py及/api/native-session/prepare：将before/after UI快照转换为公开EveEditCommand，返回原生before/candidate/commands，只有字段转换，不计算业务数值或替代引擎准入。映射船体/模式、名称/标签/技能、模块顺序/换装/状态/弹药/深渊实例、子系统、无人机、舰载机成员与部署、脑插/药剂、库存与晶体；未知库存与显式空库存/零弹量保持区别。无变化返回空命令集，不应执行apply。禁止改身份/数据源/省略策略；新增未知原生字段、重复ID或超过128命令明确拒绝，不部分提交。native_fit补传已有tags字段。

公开验证：7项映射专项通过，其中5种真实候选（驾驶员+模块+药剂+库存、晶体及模块重排、舰体模式、舰载机、引擎实际生成的深渊实例）同时核对MCP preview.analysis==直接fit_analyze、candidateHash、CLI preview全结果、apply.analysis。分析条件/备注不产生编辑命令；未跑模拟战斗。

同时修复UI保存竞态：点击即捕获快照；切换装配后旧请求回执不写进新装配；排队保存沿用首个成功回执的ID/版本，避免重复创建；所有提交内容比较，不再仅检查槽位/技能而漏掉脑插和库存改动。修复编辑历史将未声明货舱转成空货舱的问题，字段缺席原样恢复，历史对象不被后续操作修改。

验证：JS延迟请求回归覆盖切换、排队、首次ID、脑插脏状态及失败恢复；快照回归覆盖未知/空/0；59模块静态检查通过。5352隔离UI保存显示成功，120发→0→撤销120→重做0通过，未写用户5208库。接口仍r39，无引擎代码变更。原生会话尚未接管界面撤销/保存，命令映射不是全量事务接入完成；后续需完成会话身份、UI元数据及事务恢复的一致性。

## LOCAL-007 / NUI-44d：预览资源完整性

审计原生事务接口发现缺失资源被填0，现独立引擎0.190.7-ui.1/r40修复。预览资源字段允许null且有双侧state/reason；baselineAnalysis和候选analysis使用同上下文，resourceComparisonComplete/reason明确整体完整性。真实零不变。UI通道与Windows入口绑定local-r40，尚未把这些资源值接入旧界面，因此没有展示层补零兼容代码。

验证29项引擎编辑/舰载机专项、3项契约通过；UI新专项验证普通舰船→夜神及逆向的MCP/CLI完整结果一致，机库缺失为null、CPU真实0为available，baselineAnalysis与独立fit_analyze一致。无新增游戏机制、无全量/Windows打包。

原生UI会话控制器/保存仍未完成，这次先排除了接入会导致的缺值冒充0问题。上游移交登记LOCAL-007，旧r39冻结契约保留。

LOCAL-007提交对应：引擎dbb3fb4；UI 1f94c3c。

## NUI-44e：保存按钮实际接入原生事务（待用户验收）

/api/save在N引擎模式先通过公开fit_create/fit_execute(apply/save)/fit_export保存，再原子写UI装配库。首存创建显式诊断草稿，后续按前一已保存UI快照生成一组公开编辑命令；原生结果fitHash必须与同输入fit_analyze一致。库记录nativeSession的身份、版本、fitHash、源绑定及接口版本，界面元数据仍归UI。宿主不直接编辑原生会话文件。

客户端新装配预分配稳定UUID，每次保存携带事务标识；同内容失败重试保留标识。服务端保存回执绑定输入哈希，同请求重放不重复写库，不同内容复用标识拒绝。原生操作ID由装配ID、请求标识和输入哈希确定。引擎已提交但UI库写入失败时，重试复用原生apply/save回执并重新核对导出；源数据不匹配、外部版本冲突或导出快照不同均禁止覆盖库。两个存储文件不声称单一跨文件事务，以幂等重试恢复中断；失败期间旧库记录保留。用户改变失败请求的内容后若先前保存已经成功，可能收到版本冲突，须重开核对，不自动覆盖。

验证：3项真实原生持久化专项通过，涵盖首存/修改/保存、CLI与MCP导出同结果、同请求重放、请求内容冲突、UI库首次/后续写入故障恢复、非法槽位诊断草稿、外部原生修改后的CAS拒绝且库不变。JS保存控制器回归新增稳定身份/失败请求重放，59模块静态与Python语法检查通过。5352隔离页面首次存入nativeSession，重开后库存1000→1002，经真实输入事件保存；库revision5、原生revision4、双方库存1002且原生dirty=false。未写5208用户库。

范围：保存入口（含调用同API的导入）现已走原生事务；UI在编辑中的历史/预览仍未由原生会话控制器完全接管。未增加游戏机制、未改引擎源码或r40契约、未重打包Windows。该批不是全目标完成。

## LOCAL-008 / NUI-45：实时换装预览接入（待用户验收）

工作台悬停/拖动换装预览现调用/api/preview，适配器将前后快照映射为公开编辑命令，再调用fit_preview_input。输出选择、目标和防御上下文沿用正常分析适配，每次预览取引擎候选analysis展示；电容仍复用既有公开情景查询。响应附editPreview及其公开请求，可直接通过CLI重放。旧前端幽灵数值呈现保留，业务结果来自原生预览。

新增无状态入口不创建大量临时会话，不写装配库；预览中的缺资源/null/诊断保留。修复原先预览强制crystals=[]导致未声明库存变空库存的问题。无变化预览允许空命令。只读接口升级0.190.8-ui.1/r41，源数据/规则和会话格式未变，继续使用local-r40状态目录以保留已存原生会话。

验证：30项引擎编辑/舰载机、3项契约通过；4类MCP/CLI预览与直接安装分析完整对照（安装、无变化、目标、诊断），没有session文件副作用；既有3项原生保存回归及59模块检查通过。5352隔离页面150mm炮预览CPU/PG和DPS，47.89→45.61；安装后45.61，随后撤销恢复。未改5208用户库；未跑全量/Windows打包。

引擎提交152e8e9，本地补丁LOCAL-008待上游合入。实时撤销/重做的原生会话接管仍待继续，本批不是整体目标完成。

## NUI-46：原生编辑、撤销/重做与同会话保存（待用户验收）

新增native-edit-history控制器，装配变更先通过公开prepare/preview-input生成命令和候选hash，再在独立编辑会话fit_execute(apply)；撤销/重做分别调用原生undo/redo，核对回执hash和当前analysis.fitHash后确认完成。无原生输入变化的UI选择只记录界面历史，不伪造原生命令。名称/标签/驾驶员也纳入mutate和对应UI快照，避免绕过原生历史；防御/输出等分析条件仍不变成原生编辑。

提交期间暂时禁止工作台重复编辑并显示状态。明确的引擎拒绝恢复之前UI快照及历史；响应不确定时保留原请求和候选草稿，提供“重试编辑”，禁止后续编辑越过未确认操作。同一create丢失响应会inspect核对初始hash，apply/undo/redo重试不换requestId；回执与当前状态不同不能当成功。结构化错误码从HTTP保留到控制器。

保存捕获所属编辑会话，先等当前编辑完成；/api/save核对工作分析hash和UI提交一致后在同会话save，回执版本更新控制器，不丢失undo/redo。未知保存结果禁止继续编辑，允许再次保存以相同请求恢复。打开装配/刷新时按原交互清空UI历史并新建独立编辑会话，首次保存也初始化该会话，避免旧工作版本冲突；历史原生文件保留，但没有新增跨重启历史恢复界面。库中重命名当前装配后重置编辑状态，避免绕开会话。

验证：JS控制器驱动真实Python→MCP引擎，覆盖apply/undo/redo、无输入变化、不同编辑归属、create/apply/save成功后丢响应恢复、同会话保存后undo/redo以及重开再保存。3项原生持久化故障/CLI回归、保存竞态与库存快照测试、60模块静态检查通过。5352隔离页面弹量0→10形成原生revision1，保存revision2；撤销revision3工作弹量0而saved仍10，重做恢复10；改名再撤销成功；刷新再保存成功。未写5208用户库。

引擎仍0.190.8-ui.1/r41，未改引擎代码或增加机制。本批接好工作台编辑、撤销/重做和保存，不代表全应用覆盖审计与Windows打包已完成。后续继续核查全部面板和允许装配类型，不将这些有限案例当全量认证。

## NUI-47：当前引擎Windows打包验证（待用户验收）

修正打包资源：同时带入公开MCP和CLI宿主（此前仅MCP），网页新增模块由现有glob纳入，Python新增适配器由PyInstaller静态分析纳入。build.ps1增加独立输出目录；发现npm未将输出目录参数传给builder，改为直接调用electron-builder并在output/windows-r41-20260918完成构建。没有发布或安装到用户目录。

扩展显式--smoke-test模式，必须设置隔离FITLAB_TEST_ROOT。打包后的Electron使用自己的backend.exe、web资源、.NET runtime和固定SDE，测试页面分析/命令预览、真实native-edit-history模块安装、同会话保存、撤销/重做、导出hash一致；另由desktop/verify_windows.py调用包内CLI对同一saved快照导出，hash与预览相同，未授权API为403。测试后程序退出，原生会话及证据保留在隔离目录。

结果：output/windows-r41-smoke-001/verification.json通过，版本0.190.8-ui.1/r41；desktop-smoke.png视觉核对原生桌面壳与装配库正常显示。NSIS安装包生成成功，212703281字节，SHA256 d8d7ab8feebccb8ff60f9162f5dbb3e0ed537f96de5dc59780e316261a473d58，路径output/windows-r41-20260918/EVE-FitLab-0.1.2-beta.1-Windows-x64-Setup.exe。没有运行安装器的安装/卸载流程，测试的是其同批win-unpacked程序；未完成所有面板/所有机制组合覆盖，也没有将其当正式发布。60模块静态检查通过。

本批只改UI打包和显式测试入口，独立引擎源码与契约不变。全目标仍待装配类型/面板覆盖审计，继续按现有机制能力核查，不开发缺失机制或仿真沙盒。

## NUI-48：刷新性能修复（待用户验收）

保持布局及引擎契约 0.190.8-ui.1/r41，新增有界完整快照查询缓存、目录 gzip/ETag、静态资源重新验证、简中 DOM 翻译旁路、并行入口加载。业务结果仍来自公开引擎，缓存保留 null/来源/诊断，会话及随机/价格查询不缓存。未改引擎代码，无引擎补丁需要上游重放。

实测与限制见 [性能记录](performance-20260919.md)：重复分析约 1 秒降至 0.05–0.14 秒，赫卡特三次热刷新约 0.8 秒；服务重启后冷刷新仍 6.9 秒，后续待优化。缓存回归、真实原生编辑历史及 60 模块检查通过。布局未改，既有工作台编辑锁定体验尚未在本批调整，不能视为全部 UI 退化已修复。旧 Windows 安装包未包含此修复。

## NUI-49：第一阶段发布收口与状态/来源修复（待用户验收）

发布只按 phase1-release.md 的固定队列推进，暂停额外功能扩张；脚本控制的战斗沙盒仍不在范围。最新审计确认完整分享身份/上下文、部分已支持投影入口、编辑等待体验及最终安装包仍未闭环，不能只归为引擎缺口。

新增 analysisStatus，分别保留 legality、completeness、admissionIssues 和 unavailable；摘要可为 valid/invalid/partial/unsupported。原生 RESOURCE_EXCEEDED 仍阻断合法性，不把所选输出未完成混成安装错误。工作台、EFT/图片导入和分享图共用状态文字，不改布局。修复船体属性、锁定距离、无人机控制距离中 null 经单位换算变为 0；未知资源不再生成 0 值进度条或红色超限样式。

图片装配码新增 sourceBinding（engineVersion/revision/staticRule/buildNumber/indexSha256），公开结果来源进入载荷；分析、属性、估价、预览准备和保存均检查来源，不同来源明确拒绝。未改公开引擎契约，此为 UI 传输与展示附加字段；当前引擎仍 0.190.8-ui.1/r41。本批未解决分享原生身份和全部情景上下文，因此未宣称完整无损分享完成。

统一检查入口 python scripts/check-phase1.py，在临时数据目录执行并逐命令保存日志。output/phase1-gate-20260918T162557Z/result.json：84 项 Python 测试、10 个 JS 专项、61 模块检查全部通过，releaseReady 明确为 false。随后扩展跨来源属性/估价/预览检查并单独重跑 source_binding 和空值显示专项通过。旧 integration 测试写死 0.190.1-ui.1，修正为核对当前固定基线并增加接口 revision 检查，不放宽源校验。

实际 5208 页面显示“装配合法 · 当前结果完整”，原赫卡特 468.3 DPS 不变；仅临时只读标签，无用户装配修改。未改引擎，无新增上游引擎补丁；未重打包或对外发布。

## NUI-50：第一阶段引擎专项审计（只读）

当前独立副本 0.190.8-ui.1/r41、规则 v54、SDE3503375；未检查或修改主引擎/历史副本。使用511项当前技能V，17种装备各在线/主动共34个原生fit_analyze查询，另2个错误槽位查询。17个主动案例及2个槽位案例CLI完整结果/错误对象与MCP一致。

确认3项引擎P0：零速度模式导致EVE_MOTION_PARAMETERS整份分析失败；特殊主动模块因电容效果元数据要求导致EVE_CAPACITOR_EFFECT整份失败；未知主动效果提前返回导致资源全空，甚至已知SLOT_UNAVAILABLE错误消失。详见phase1-engine-audit-r41.md，附请求、回执、源码定位、验收条件。未将运行时采矿/骇客等机制列为必做，也不靠把未知值补0规避问题。没有新确认的公开接口缺失；已有LOCAL补丁不得重复报缺。

证据包output/phase1-engine-audit-r41.zip（3,862,662字节，SHA256 b86781409c3658d934eef87e9731e2dc5b503e1c41a57ec57d64a057bef124ab）；精简证据归档evidence/phase1-r41-summary.json；复现脚本scripts/audit-phase1-engine.py。本批无运行时代码或引擎更改，不向其他任务发送消息。17类并非全目录/全组合穷尽证明；可以立即把3项确认问题交给引擎冲刺。

补记用户最新关卡：技术能力闭环之后，用户还要亲自主导UI重构和交互设计，完成验收后才能准备发布。技术检查不代表产品发布资格。

## NUI-51：完整装配文件与原生身份分离（待用户验收）

新增fitlab-static-fit-1文件交换及/api/fit-package/export/import，临时入口为装配库条目右键导出、底部空白右键导入。导入验证成功后创建新记录，不覆盖原库。文件保留原生nativeFitId；本地UUID改变不改变原生输入/fitHash。新图片分享也带原生身份，但图片仍保留原无情景设计。

完整文件保留主装配字段、目标/传电/毁电与其他预设的引用快照、输出/防御/窗口条件、报价来源快照及公开MCP请求。导入重算完整原生分析、电容、估价并比对摘要；版本不一致、损坏或结果不一致拒绝。冻结来源优先于接收端库，工作台明确提示；不将参考来源写入用户库。修复浏览器JSON将1.0重写为1导致的误判，摘要按语义JSON数值归一化；没有游戏公式。

验证见fit-package.md：16项相关Python回归、图片原生往返与编辑历史通过；新增晶体/方案/模式/舰载机组合及来源缺失专项通过，含Node真实JSON往返和CLI完整分析对照。隔离58485浏览器选文件、验证、保存、重开、真实文件下载并再校验通过，output/package-qa-01/verification.json记录本地ID不同/原生身份与hash相同。浏览器下载事件监听超时，但实际文件存在且内容重新导入验证通过。最初5357端口被系统拒绝绑定，改用系统分配的空闲端口，不涉及权限绕过。62模块检查通过。

本批未修改引擎、未发布；完整文件体积超限不截断，原生顶层错误仍受已确认引擎P0阻断。实例后续重排生命周期及全部组合尚未认证；冻结来源主动更新交互留给用户后续UI整理，不声称第一阶段整体完成。新UI均为能力验证，必须经过用户主导的重构与验收。

## NUI-52：0.200/r43整合候选隔离接入（待用户验收）

已核对LOCAL-001～008整合情况，并用真实FitLab专项验证。新引擎未改源码，默认旧后端未切换。新隔离工作区56432使用output/integration-r43-ui-001的独立UI/引擎状态；旧v54会话没有迁移到v59。

本次重跑36个原生P0案例/19个CLI对照、5个MCP草稿往返及CLI草稿往返均通过。FitLab统一门禁首次88项Python中87通过，唯一错误是曲线测试写死local-r36契约路径；改为实际交付revision后2项曲线补测通过。10个JS专项通过；额外状态/显示/电容和62模块检查通过。证据与补丁映射见integration-r43-acceptance.md，不改写首次失败日志。

UI消费零速不适用原因、validationState、电容贡献缺项和独立容量/峰值，不把null当零、不隐藏已知资源。实际堡垒及错误槽位页面验证通过。原三P0的全局失败/诊断丢失已在本候选修复，特殊效果仍存在明确保守隔离，不宣称全部机制已完成。尚未Windows打包或发布，用户主导最终UI重构的关卡保持。

## NUI-53：浏览器分类与弹药就地编辑（待用户验收）

按用户四处标注调整：当前目录新增型号优先归入同SDE装备组已有市场功能分支，缺分支时使用舰船装备/军火和弹药等统一根；官员/势力仍是分支内科技类别，不另开“装备”根。分类为UI导航策略，不冒充游戏公式。

新装填和全部装填用原生magazines.capacity填满，晶体实际创建damage=0的实体，显示100%剩余耐久。当前普通工作台未配置弹量/晶体的槽位首次分析后作一次可撤销初始化；版本绑定的完整文件不自动覆写未知库存。满仓是用户明确的新装默认输入，不是将引擎null结果伪装为满仓。预览采用相同默认输入。

弹药末尾改为无边框普通文字，显示数量/容量或耐久百分比；点击就地数字编辑，Enter或失焦提交，Esc取消，支持键盘聚焦。晶体备用管理对话框也改剩余耐久百分比与全新/半耐久说明，按引擎hitpoints转换damage，0%不允许装载。原生装配、撤销、保存仍存实际数量与晶体身份/损伤。

隔离58485页面实际验证100%、160/160默认值，修改为60/160与50%，刷新保留；晶体身份/百分比、真实原生编辑历史和62模块检查通过。目录HTTP核对官员装进入舰船装备功能分支。没有改游戏引擎或做新UI布局。当前56432目录服务已重启，前端刷新后生效。完整文件未知输入继续保留；引擎不能给出容量时明确失败，不自行推算容量。

## NUI-54：电容主面板按稳定性展示（待验收）

主面板取消观察窗口入口。稳定时绿色“稳定 · 百分比”，不稳定红色；平均稳定结论不再被窗口内首次付款失败覆盖。容量、峰值回充和净消耗保留，窗口、最低/结束电量、逐次付款及曲线全部收进“电容详情”。周期查询不可用时不显示无效窗口选择，缺项直接显示装备名称（如堡垒装备I完整耗电数据缺失）。未知负载不得以本舰局部稳定结论覆盖。

当前公开平均模型没有预计耗尽时间，所以不稳定时明确提示预计续航尚未提供，不拿窗口首次付款失败/首次归零冒充平均续航。没有新增前端耗尽公式。数据来源、有限供给/外部情景限制仍保留在详情。观察时长只用于图表与周期详情。

新增test_capacitor_summary.mjs验证时长改变不改稳定标题、离散付款失败不覆盖平均结论、未计入项保持未知、缺项装备命名；既有状态显示与62模块静态检查通过。实际56432页面确认主面板无窗口、显示“暂无法确定”和“缺少堡垒装备I的完整耗电数据”。仅前端展示调整，未改引擎或重打包。

## NUI-55：恢复旧版右侧属性栏结构与悬浮说明（待验收）

参照旧引擎最后版本0bd7823，恢复紧凑标题、标题内DPS/EDPS及HP/EHP/针对抗切换、分层抗性条、总HP/EHP、被动回盾、质量/惯性、带宽摘要和感应强度。输出口径和分项说明收进输出详情，电容保持第一块。当前220px属性列已目视检查。

新增native-panel-detail.js集中格式化原生trace，右栏属性支持基础值、装备/技能名称、修改前后值以及二级操作/叠加说明；抗性可继续查看共振来源。电容标题/容量/回充与输出分项增加解释入口。单位转换同时作用于最终值、基础值与逐步值，不重新执行游戏公式。未知值保持—，部分输出与未计入原因仍显示。复用既有悬浮锁定、逐层展开和引擎DPS曲线组件。

接口未变：N 0.200.0 / r43 / static v59；只改UI展示，未修改引擎。保留既有查询、选集、完整性和情景口径。尚非旧版全部指标完全对齐：含换弹总DPS、齐射/伤害占比、维修汇总和预计电容续航本轮未恢复，不使用旧公式或UI独有计算拼补。后续仍需按公开引擎口径逐项对齐与用户验收。

验证：63模块检查、test_analysis_status.mjs、test_native_attribute_detail.mjs、test_capacitor_summary.mjs、test_native_panel_detail.mjs通过。56432实际装配确认攻击366.06、武器338.34、无人机27.72；总HP30250、带宽10/25；护盾8625→护盾管理二级面板显示基础6900与25%修正。旧版5207对照页面未改动。没有发布或重打包。

## NUI-56：电容续航与旧版悬浮曲线（待验收）

修正NUI-54/55的接入遗漏：公开capacitor_scenario已支持firstFailedPaymentSeconds，旧版续航摘要实际也是首次无法支付启动耗电。平均模型未提供耗尽时间，不等于无法显示周期供电续航。完整且平均不稳定、默认查询尚无付款失败时，自动追加一次最多3600秒的公开查询。保留最终fit/query用于MCP/CLI重放；追加查询失败时保留原观察和失败原因，不伪造续航。

不稳定且查到失败时标题显示“10分52.5秒 · 供电不足”等；不等同电容归零，不冒充平均负载耗尽时间。平均稳定与周期失败仍分别表达；缺失平均条件保持未知。尚无终点时保留续航待确定及悬浮查询范围。

右栏恢复旧版容量、回充时间、峰值回充、模块耗电、净耗电；移除展开式电容详情与窗口下拉。标题悬浮展示曲线、百分比/GJ探针，条件和来源折叠在浮层。曲线使用原生采样，存在失败事件时绘制至该原生事件；没有额外游戏公式。模块耗电支持逐项周期/单次耗电说明。

无引擎修改，接口仍0.200.0/r43/static-v59，使用现有公开capacitor_scenario。实际56432当前帕拉丁查得652.5秒首次供电不足；已截图确认标题与悬浮曲线。64模块检查、3个JS专项通过；自动延长/未知/零秒失败/查询失败单测3项、实际MCP与CLI同输入对照1项通过。默认最多追加一次查询。此轮重点修正电容，并非宣称所有旧版右栏指标已经完全还原。

## NUI-57：整栏采用旧版设计，不自行新增入口（待验收）

按用户最新指示，以0bd7823旧版右栏为交互基准：攻击常驻武器/无人机/含换弹/齐射/四类伤害条，防御常驻三层抗性、总量、三类维修与回盾；机动恢复质量、惯性、起跳、跃迁、信号半径顺序；锁定先目标数后扫描分辨率，无人机使用原最大操控数量文案。抗性条恢复整数精度，DPS恢复一位小数；起跳悬浮仍说明静止75%阈值，不保证跃迁准入。按旧版288px对照列宽检查并保存当前浏览器宽度。

撤去输出详情/口径下拉、独立舰载机分项行、计算范围折叠面板、估价明细折叠入口。舰载机贡献与缺项仍在总DPS悬浮；诊断移到既有装配状态悬浮；估价来源、缺项和快照留在估价悬浮。属性说明恢复来源行样式和三级修正明细。既有对外效果分组使用原生能量、远修与电子战属性值。没有新增用户操作入口；今后确需额外入口先由用户确认，已写入AGENTS.md。

齐射使用现有grouped_reading对已选舰载武器的volleyDamage/appliedVolley/effectiveVolley按原生aggregationKey汇总，已验证与公开fit_analyze选择合计和CLI sde-fit --metrics一致。内部分析响应新增legacyInspectorOutput，不改原生引擎契约（仍0.200.0/r43/v59）。本舰单个普通维修器直接取activeHpPerSecond，禁止沿用旧前端修量公式。

未完整接通的含换弹全选集合、四类DPS比例、多维修器合计、EHP/s折算保留旧行显示—及悬浮原因，不代表引擎必然缺机制。持续维修/采集等旧版条件能力仍需按各自原生口径逐项接通，不能拿旧公式补数，也不另加新设计。本轮不宣称旧版所有数值能力均已恢复。

验证：64模块检查，状态/悬浮/电容/估价4个JS专项通过，齐射同输入MCP/CLI验证通过。56432实际帕拉丁显示196.9 DPS、959.2 HP齐射、112.44 HP/s装甲维修；常驻行、整数抗性、惯性0.04630与288px排版目视检查通过。旧版对照页未修改，无发布。

## NUI-58：选集伤害、换弹与维修原生汇总（待验收）

当前独立整合副本追加UI-LOCAL-009，引擎提交f3bf8f5，0.200.1-ui.1/r44/static-v59；未改源主库和历史副本。`fit_analyze`与CLI `sde-fit --metrics`统一新增analysis.inspector（static-inspector-v1）。UI直接读取已选集合的分类型DPS、比例、含换弹参考输出、本舰维修HP/s及按来伤折算EHP/s；不新增前端游戏公式或用户入口。

修复智能炸弹kind=smartbomb漏入武器小计/齐射的问题。未知、有限次数副武器的无限补给策略缺失、零分母比例、缺失静态覆盖均保留null与原因；确实没有维修器且覆盖完整时显示真实0。修理合计排除远修并使用引擎ActiveHpPerSecond；HP/EHP/针对抗沿用旧控件。接口新增汇总没有增加常规分析MCP往返。

换弹口径为满弹仓、无限备弹、固定初始预热，并非有限库存战斗平均。当前势力晶体若原生固定投影不支持，仍为INSTALLED_CRYSTAL_NOT_CONSUMABLE_MAGAZINE，中文说明保留；不得拿名义DPS填空。持续维修周期、采集及更广晶体策略仍不宣称已接完，亦不开发缺失游戏机制。

验证：引擎6个新增专项、3个公开契约/实际MCP发现、81个相关回归全通过；UI 2项实际引擎测试（含整份inspector CLI/MCP对照）、3个JS专项及64模块检查通过。实际56432当前单激光器加无人机帕拉丁显示172.3总DPS，电磁112.1/65%、热能60.2/35%、动能爆炸真实0；未装备维修器时三类维修为0，回盾12.65 HP/s。布局、入口保持旧版。

接口与兼容性：r43冻结契约未覆盖，r44新增字段包含嵌入分析快照的schema。静态输入v59不变；旧来源绑定分享文件仍按既有严格检查拒绝，不改写版本；不迁移战斗检查点。运行读取UI-LOCAL-BASELINE.json。主引擎交接/升级防覆盖清单见独立副本docs/ui-local-changes.md。

## NUI-59：返回装配库不等待原生保存（待验收）

旧navigateFitPage先await persistFit再切换可见页面，导致保存耗时全部表现为返回按钮卡顿。本次先保留本地草稿、立即切换并绘制已有库记录与草稿，再等待保存和权威库刷新。正在保存的那一条暂不可重新打开/右键修改，其他条目不受限制；按装配ID跟踪并发离开保存。回执只更新对应且未改变内容的缓存条目，导航版本变化后不得用旧请求刷新当前页。

test_library_navigation.mjs实跑导航函数：保存悬而未决时页面已经切换并绘制；失败保留草稿；离开后回执不覆盖另一艘船。既有保存并发专项和64模块检查通过。56432实际点击后已进入装配库，同时当前草稿条目仍在后台保存，确认不再等待保存完成才切页面。未修改引擎、接口或UI布局，不宣称减少了底层保存耗时。

## NUI-79：舰船特性与技能要求（待验收）

舰船详情新增特性、所需技能页签，默认特性；每级收益按技能分组，role独立显示船体特有加成。catalog_item.traits及来源接入items展示代理；直接技能前置按固定SDE requiredSkillN/requiredSkillNLevel字段读取，保留类型/等级字段来源。技能链递归及角色满足状态本次不展示，不冒充完整技能准入判断。文本去除SDE标记并转义输出。原基础/装配属性与效果页签保留。

验证：真实r54裂谷级返回两条traits、米玛塔尔护卫舰3329等级I；67模块检查通过。未修改引擎或计算规则，新增模块ship-info.js。UI交互与外观待用户调整。

## NUI-78：装配库空白新建与分类定位（待验收）

修复追加：picker模式参数与drawTags内标签弹层局部变量picker重名，引发暂时性死区异常，导致主库加载失败。模式变量改为pickerMode，标签弹层保留独立名称；浏览器刷新后恢复1/1装配。静态模块检查无法捕获这种运行时遮蔽，补以实际页面流程核验。

空白右键菜单在粘贴前增加新建装配。显式选中舰船直接使用原创建表单；否则复用createLibraryTree的picker模式选择船型，再进入同一表单。分类初始展开仅包括所选路径的祖先及分类本身，子分类保持收起；选择器滚动定位所选分类。不读取或写入主树展开偏好，不移动主库搜索框，不注册重复全局监听。66模块检查通过。

## NUI-77：r54能力发现替换（待验收）

用户选择先完成能力接口替换。新增按需catalog_item能力代理/浏览器缓存，模式、子系统候选、模块状态、弹药声明和晶体宿主政策读取新接口；显式Active不再降为Online。删除fighterHull启发式与固定5架控件。细节及仍存快速预筛边界见integration-r54-acceptance.md后续批次。引擎未修改；测试与浏览器完整性边界如实记录。

## NUI-76：r54独立接入首批（待验收，非全量完成）

详见integration-r54-acceptance.md。新端口56454及独立状态；写入舰载机位置、按管内占用显示分类配额、取消舰载机/无人机互斥与武器duration白名单。29项相关Python测试、65模块检查及新r54实际查询通过。能力发现全面替换、炸弹政策UI贯通等仍待继续；未修改引擎代码、未迁移旧库。

## NUI-75：末日武器静态阻断核查（待引擎）

四种种族定向末日，在对应泰坦、全部技能V、Active单高槽下均复现UNKNOWN_STATIC_FOOTPRINT（4489–4492），输出不可用且原生resources空。已登记engine-handoff-r44.md ENG-44-12，区分静态资源隔离与末日伤害机制缺口，未假定所有末日家族同因。本次只读验证和文档更新。

## NUI-74：重型副武器具体核查（待处理）

新增engine-handoff-r44.md ENG-44-10/11：独眼巨人II鱼雷齐射返回EVE_FIGHTER_ROCKET_BINDING；白蚁II实际还有微型跳跃引擎和发射炸弹，炸弹有原生元数据但DPS/齐射要求政策。UI按duration属性白名单漏掉2349炸弹，是CAP-08具体漏接；本次未修改行为。之前“火箭/鱼雷齐射已支持”的泛化表述不准确，轻型和重型必须分别验证。

## NUI-73：能力驱动接入审计（文档完成，改造未执行）

审计报告见capability-driven-ui-gap-audit.md：14项发现，区分UI已有能力漏接、公开发现/状态契约缺口和待核实机制；已关联engine-handoff-r44.md的ENG-44-06～09。核对r44 catalog_type_details已有无人机/舰载机capabilities、fit_preview_input已存在，避免误报整个机制未开发；确认UI机库互斥、固定5架操作、模式映射与装填/状态的本地判断。文档提供分阶段改造和跨类型验收矩阵。此次未改运行代码、未修改引擎，也不宣称完成全量特殊情况回归。

## NUI-72：指挥航母舰载机入口与导航（待验收）

UI原fighterHull只认547/659，遗漏5120指挥航母。改为按原生目录2055舰载机舱容量识别，继续读取分析fighterBay实际管数/分类配额。目录中无旧市场路径的5120舰船在UI导航分组补旗舰层级，并标记fitlab-capital-navigation-v1，不声称这是当前官方市场树完整复刻。引擎不改。

实查92822–92825为救赎/西莫夫/盖亚/伊米尔，目录均有4管、普通无人机带宽0；救赎空技能只读/api/analyze返回fighterBay容量65000m³、4管、light2/support4/heavy0，技能不足诊断仍保留。65模块检查通过。此项是UI漏接，不是无舰载机能力。

## NUI-71：舰载机装载配额归属核查（引擎契约阻塞）

已登记engine-handoff-r44.md的ENG-44-06。原生静态模型只有Deployed，实跑管内待命与备用同身份中队映射得到完全相同请求，无法由UI正确传递管位状态。分类装载规则的外部证据仍需核实；不能把待命强制设为部署来绕过缺口。未改UI或引擎行为，等待契约区分管位/备用/部署后接入。

## NUI-70：角色技能点显示缺口（部分修正，待引擎）

skill-points.js原实现固定返回横线。改为明确空技能列表或全部level=0时展示空和0；缺失skills及有技能角色仍保留未知。当前r44副本src中未找到技能点机制/公开查询，不能依据等级在UI独立实现SP公式。后续引擎需提供技能快照的逐项/总技能点及来源、完整性，UI再接入；本次不是完整修复，不应关闭总SP验收项。角色列表、详情、装配驾驶员选择共用该显示函数。

## NUI-69：空装配绘制零线（待验收）

按用户修正，NUI-68空态说明改为恒零曲线。距离、信号半径、角速度页签均显示0水平线和当前0DPS/EDPS；沿用NUI-67完整空选集证据，不虚构引擎采样网格或峰值。坐标横轴只标维度与方向，不声称有限采样范围；有攻击来源时继续显示原生采样。65模块及空输出专项通过。

## NUI-68：空攻击选集悬浮曲线（待验收）

复用NUI-67已验证的empty_selected_sum条件，悬浮曲线显示“当前未选择任何攻击来源，输出为0；暂无可绘制的攻击曲线”，不再发起注定返回INCOMPLETE_OUTPUT_SELECTION的采样请求。保留原曲线页签和面板结构，不伪造采样点；非空或不完整选集仍走原生查询与诊断。65模块、空输出专项检查通过。引擎和接口不变。

## NUI-67：无攻击来源显示零（待验收）

修正右侧总DPS、武器/无人机分项及齐射把空选集显示为横线的问题。仅在公开inspector四类伤害均available、无staticBlockers、对应选集为空且无分组/排除项时，按展示聚合的空和约定显示0。保留原生empty_selection/null回执，不将不可用贡献转为0。总伤害为零时比例按图形展示约定显示空条和0%，悬停保留ZERO_BASELINE（比例无定义）；该0%不是新业务比例。没有新业务公式或引擎改动，MCP/CLI可取得相同选集及完整性证据。

验证：空和展示专项覆盖空选集、非空不可用项、缺失报告、静态阻断、排除项和null读数，65模块检查通过。接口r44不变。

## NUI-66：舰载机拒绝安装后的交互恢复（待验收）

夜神安装修道士II由原生fighterClass.support限制拒绝（1/0），原浏览器拖动结束仅清空drag，遗留fighter-drop虚线。统一清理拖放状态：落下前、拖动结束、取消、失焦及选择管位时清除高亮，拖过新管位时移除旧高亮。校验忙时给出提示。管位增加独立单选标记与aria-pressed，重复点击或Esc取消，切换管位不受安装失败影响。原生配额错误转为中文并显示原生已用/上限；不修改引擎限制。

验证：65模块和舰载机输出选择专项通过。实际夜神新装配尝试修道士II，被拒绝并显示支援中队1/0；随后切换1→2管位和重复点击取消的aria状态均正确，未写入非法装配。未修改引擎或公开接口。

## NUI-65：装配库舰船右键菜单（待验收）

舰船浏览器的具体舰船条目增加新建装配、舰船详情，复用公共菜单定位/关闭/键盘导航与原有创建表单。创建显式传入右键舰船，不依赖原筛选；详情增加基础资料模式，不请求或混入当前编辑装配的ship属性快照。支持ContextMenu/Shift+F10入口。无引擎及接口变化。

验证：65模块检查通过；实际右键夜神级显示两项菜单，新建表单正确预填夜神级（测试未提交），舰船详情成功显示夜神级说明与基础属性。

## NUI-64：舰载机市场层级与分类图标（待验收）

修正NUI-63自行增加顶层入口的问题。按现有导航源SDE3248221的marketGroups，采用无人机(157)→铁骑舰载机(2236)→航母铁骑舰载机(2410)→轻型(840)/后勤(2239)/重型(1310)，其后保留科技/势力筛选。全部53个当前舰载机typeId与该SDE的types.marketGroupID逐一核对，无缺失，分类数量24/12/17。分类节点均使用原始iconID1084及已有assets/market-1084.png。导航来源写入展示对象；仅沿用历史市场导航，不影响当前SDE3503375业务数值。

验证：65模块检查通过，浏览器搜索掷矛手显示完整层级、T1/T2/势力分组和正常加载的分类图标。r44引擎与接口未改。

## NUI-63：舰载机并入标准装备树（待验收）

取消只有点击发射管才进入的独立简化列表，将现有/api/fighters目录并入普通装备树。无筛选时常驻铁骑舰载机入口，按轻型/支援/重型、原生metaGroupId对应的科技/势力分类展开；搜索、展开记忆和物品行使用普通树实现。点击发射管仍筛选53种舰载机，复用同一分类。保留舰载机专用安装/拖动/备用/右键操作；非航母可浏览详情，但安装禁用。r44引擎与接口不变。

验证：65模块检查通过。实际浏览器验证无管位筛选时可展开入口、轻型下一级科技/二级科技/势力分列、T2叶子物品右键可装入空管/加入备用/查看详情，切换发射管筛选后分类与展开状态保留。未改动用户装配。

## NUI-62：舰载机附加武器默认计入（待验收）

新安装或替换中队完成原生校验后，从现有outputContributions中选择有可用读数的附加武器（包括有限弹量火箭），与中队及outputMetric一起进入同一次编辑快照。选中有限能力时使用loadedCycleDps；不能计算的能力不默认选入。移动、调整数量、参战切换不重新初始化，用户手动关闭的选择保持。仅展示选集默认策略变化，r44接口和引擎公式无修改。

验证：选择专项通过，覆盖新中队默认选择、不可用能力排除、备用预选、手动关闭不重开、有限口径切换、撤销恢复；65模块检查通过。

## NUI-61：舰载机分类配额（待验收）

分类限制改为已部署中队数/上限，读取现有公开分析 resources 的 fighterClass.light/support/heavy，待命与备用不计入。缺失已用数显示—，不当作0。复用挂点 limit-free/full/over 颜色，分别表示未满、已满、超限；0/0按已满显示。悬停说明统计范围。仅UI展示调整，引擎与r44接口不变。65个浏览器业务模块检查通过。

## NUI-60：舰载机有限次数武器直接选择（待验收）

用户确认复用现有武器按钮完成口径切换，不恢复已撤销的独立下拉。只有原生nominal读数为FINITE_ABILITY_USE_LOADED_CYCLE_BASIS且loadedCycleDps有可用有限数值与aggregationKey时，允许从名义口径直接选择。点击将武器选择和outputMetric一同写入草稿；任一中队（含待命预选）仍选择有限次数能力时保持loadedCycleDps，全部取消后恢复nominalCycleDps。待命单位仍由既有选集逻辑排除，不混入当前DPS。

菜单说明有限弹量周期假定可发射，不代表无限持续输出；主属性悬浮继续说明口径。其他真正不支持的能力保持禁用。outputMetric补入编辑快照，保证撤销/重做同时恢复选择和口径；保存仍沿用已有装配字段。未改变引擎或弹药库存。

验证：选择策略专项覆盖可用/未知/缺值、多中队保留、最后取消、输入不被修改及撤销重做；现有编辑快照专项、65模块检查通过，真实引擎primary_and_finite_basis专项通过。

## NUI-80：装配库入口与新建标签（待验收）

删除列表上的新建装配按钮和页头文本导入按钮，保留右键新建；页头图片导入改为扫描码 SVG 图标。新建装配命名窗口右下角增加从文本导入、从图片导入，复用现有导入流程。标签改为可删除的标签块，支持 Enter/逗号添加、去重、空输入退格删除及已有标签建议，提交时保留尚未确认的输入。

共享舰船树接入四大帝国标志，SDE 势力标识为艾玛 500003、加达里 500001、盖伦特 500004、米玛塔尔 500002；图片来源为 EVE 官方 Image Server corporations/{id}/logo。当前仅接入已核实的四大帝国，不代表所有势力图标已覆盖。

验证：Playwright 在 1045×912 下验证右键新建、选船、标签去重/删除/退格及三个导入入口；四种势力图片 naturalWidth 均大于零，无页面脚本错误，无横向溢出。截图 output/library-new-fit-qa.png。仅 UI 改动，r54 引擎与公开接口版本不变，无新增业务计算。等待用户验收交互和视觉。

### NUI-80 交互修订

用户指定新建标签必须采用装配工作区原有＋交互。将原有交互抽取至 fit-tag-input.js，供新建及装配页共同使用；移除上一版常驻输入框和建议区。Enter/失焦确认，Esc取消，清空删除，40字上限与原装配页一致。仅UI组件复用，无引擎或接口变化。浏览器验证添加、取消编辑、删除，68模块检查通过。

## NUI-81：角色等级技能点接入（待验收）

角色管理列表、角色技能页及驾驶员选择器接入公开 skill_points。全V（511技能）显示641,792,000 SP，无技能0 SP；编辑等级重新请求同口径总量，UI仅做数字格式化。未知/部分结果显示—并在悬停保留原因，不以已知小计冒充总量。悬停说明这是等级对应最低累计SP，不含账号训练进度或未分配SP。

引擎独立副本0.210.1-ui.sp1、契约55（r55-ui-sp1）、34工具，原静态v59及SDE3503375不变。本次用户特许新增机制，补丁UI-LOCAL-010登记于引擎docs/ui-local-changes.md，下一次升级必须核对。新增POST /api/skill-points仅转换技能数组为原生字典并转发，保留原生完整回执，不计算公式。原引擎交付基线与历史契约不改，新增UI-LOCAL-BASELINE.json绑定本地派生。

验证：引擎逐级、空集、混合集合、全V、未知/非法输入CLI/MCP对照通过；3项契约通过；UI68模块与Python编译通过；真实浏览器角色列表、详情、选择器的0和全V均显示正确，无页面异常。证据output/skill-points-characters.png和skill-points-picker.png。UI交互等待用户验收，不发布。

## NUI-82：官网授权真实角色导入（待真实账号验收）

修复SSO发起端56454、固定回调5207落入旧进程的断链。当前服务通过同进程回调监听共享PKCE/单次state/browser-cookie上下文，5207回调完成后明确返回发起端；非回调访问5207重定向到当前角色管理。占用时给出明确错误，不把用户送进错误实例。此次暂停5207旧对照服务以释放已注册回调端口，未改历史源码或旧角色数据。

授权仅esi-skills.read_skills.v1，令牌只在一次请求中使用，不写磁盘；访问日志不输出授权码。保存完整技能快照：active/trained等级、逐技能SP、total_sp、unallocated_sp、URL/抓取时间/HTTP日期/过期时间/响应SHA256。导入前用公开character_skill_snapshot验证，按eve:角色ID更新同一记录。装配使用活动等级；角色总SP用官网值，不能用最低SP代替。失败和取消保留原因并不写入角色；重新授权刷新快照。

UI改动只在已有入口、显示与悬停说明：角色管理＋→从EVE官网导入；官网角色右键重新授权更新；列表/详情/驾驶员选择器使用真实SP。复制为自定义角色后使用等级SP，不继承账号实际SP口径。

无头：新增POST /api/character-skills转发公开MCP。python eve_sso.py --library 路径/library.json --character eve:ID --out snapshot-query.json 导出同输入；随后sde-character-skills --data DIRECTORY --query snapshot-query.json，或MCP character_skill_snapshot(query=同JSON)。文件含用户角色数据、不含OAuth凭据。

引擎0.210.2-ui.sso1/本地r56-ui-sso1/35工具，LOCAL-011依赖LOCAL-010。静态v59/SDE未变，未来升级须核对上游合入，详见引擎docs/ui-local-changes.md。

验证：六项SSO专项（PKCE浏览器绑定/单次state/过期/缺权限、导入再导入、固定5207→发起端回跳、取消不落库）通过，使用合成身份+真实引擎查询；引擎九组CLI/MCP快照对照及三项契约通过；68模块/Python编译通过。真实浏览器确认5207跳回56454、官网入口到达login.eveonline.com。官网要求真人CAPTCHA，已停止自动化；真实登录、同意授权、账号返回尚待用户亲自验收。

## NUI-83：方案新建入口（待验收）

空编辑区改为大加号与“创建新方案”的整块槽位入口，使用轻渐变、虚线边框和悬停高亮/柔光反馈，支持键盘焦点。方案库右上角新增＋，两个入口均调用既有newPlan且folder为空，创建最外层方案，不随选中的分组改变。复用原有未保存确认和命名流程；减少动态效果偏好下关闭动画。仅UI，不改引擎接口。

68模块检查通过；浏览器验证空态槽位点击与命名入口，1045×912截图output/plan-create-slot.png和plan-create-slot-hover.png。待用户视觉验收。

## NUI-84：方案加成词条面板（待验收）

在脑插与增效剂方案编辑区右上角加入“方案加成”小窗，独立滚动；窄屏移至工具栏下方。按属性及目标条件显示百分比/固定增量，收益与负面方向用不同颜色，悬停可见来源物品、选中副作用、套装/角色修正提示与适用条件。舰船和装备词条优先于角色学习属性。

现有方案分析请求改为公开booster_plan_summary，返回原分析和summary，UI仅格式化/排序。安装、卸下、角色切换、副作用选择及随机服药沿原150ms合并查询更新，旧响应按token丢弃；未知不填零，空方案“暂无加成”。未新增DPS/EHP、假舰船或战斗UI。

引擎本地0.210.3-ui.affixes1/r57-ui-affixes1/36工具，补丁LOCAL-012；静态v59与SDE3503375不变。具体上游移植清单在引擎docs/ui-local-changes.md，升级时核对LOCAL-010～012。

验证：引擎6组CLI/MCP汇总对照及真实装配速度交叉验证、3项契约、20项药剂回归通过；UI3项方案接口/回执测试及69模块通过。真实浏览器验证完整高级蝰蛇+24.738%，关闭蓝丸护盾副作用对应负词条消失，重新打开恢复，卸Omega速度变为+7.770%。1045×912面板截图output/plan-affixes-qa-final.png。测试草稿仅在隔离浏览器中，未保存到用户方案库；视觉待验收。

## NUI-85：方案加成悬浮球（待验收）

将工具栏内固定加成面板移为独立悬浮球。默认收起，点击/Enter/Space展开，点击球或面板收起按钮关闭，Esc收起；球可拖动，拖动不误触展开，拖动中右键/Esc取消。记忆位置和展开状态，窗口缩放时限制在视口内，展开面板独立滚动。仅在脑插/增效剂页且有方案时显示，数据查询与词条计算沿用原接口。

工具栏恢复紧凑单行，去掉144px高度及右侧占位。1045×912实测工具栏32.5px，编辑内容区358.5px。浏览器验证点击展开收起、拖动后保持展开、刷新位置记忆、切页隐藏、键盘和700×650边界；70模块检查通过。截图output/affix-orb-collapsed.png及affix-orb-expanded.png。仅UI改动，无引擎/契约变化，待用户验收。

### NUI-85 修订：面板拉伸与恢复默认大小

方案加成浮层支持四边/四角拉伸，记忆宽高及相对悬浮球位置。右上角替换为恢复默认大小SVG图标，点击恢复330px宽和按内容自动高度（上限360px），保持展开。内容区随尺寸伸缩滚动；视口缩小自动限制边界。右下角支持方向键，拉伸中Esc/右键取消。点击悬浮球和Esc仍可收起。

验证：浏览器实测330×76.5→437×213.5，刷新保留，恢复默认后尺寸还原且保持展开；键盘与小视口边界通过。70模块检查通过，无引擎改动，待用户验收。

## NUI-86：脑插与增效剂方案文字/图片分享（待验收）

更多菜单增加分享文字和分享图片；方案库新增导入入口，支持文字、文件、图片拖入与截图粘贴。FITLAB-PLAN/1自定义格式保留Type ID、槽位、副作用Effect ID、技能快照及有效随机回执，并绑定SDE/静态规则和原生planHash。匿名化计算角色名称，过滤本地记录与官网身份；导入为最外层新记录。

分享图包含图标、方案词条、当前副作用和可还原二维码，支持保存PNG/复制图片。图片上数值直接采用现有引擎summary；多码/校验/解压边界见plan-sharing.md。新增只读导出/导入校验HTTP适配，无引擎或MCP/CLI契约变更。通用扫码函数仅增加可选格式解析器，原装配调用默认不变。

验证：4项Python真实引擎分享测试、JS文本/多片/损坏测试、原装配分享回归和72模块通过。浏览器验证更多菜单、文字复制区域/校验、PNG生成下载及多二维码图片读回。独立浏览器测试草稿未写入用户方案库，持久化新记录通过隔离库测试。图片样例output/plan-share-final.png。待用户验收视觉和交互。

## NUI-87：晶体装填候选污染当前装配修复（待验收）

根因：currentFit只复制slots，crystals/cargo等嵌套对象仍与活动fitRecord共用。批量fillNewAmmo在校验候选前写入晶体实例，使当前模块仍未装弹、库存却已挂上新晶体；同时mutate的before快照会被后续卸载修改，原生编辑出现FIT_INVENTORY_CRYSTAL_MOUNT。

改为cloneCurrentFit对整个输入生成独立快照，覆盖批量装填、默认晶体初始化、装弹数量编辑及事务before。填弹前统一解除不匹配挂载；restoreFit使用已克隆记录的slots，避免修改装配库原记录。加载已污染草稿时将不匹配晶体移为货舱备用，保留ID、typeId和损伤，提示修复，不放宽引擎规则或丢弃晶体。

验证：幽灵级17718＋三门重型脉冲激光器II3520，批量爆燃M12814成功；撤销恢复无弹药，重做恢复三枚实例；整组换灼烧M12818成功，旧晶体成为备用。原损伤0.2的晶体保持不变。浏览器无JS异常，候选/事务快照隔离、晶体操作与编辑历史专项通过，72模块检查通过。未改引擎接口/代码。

### 2026-09-20 性能联调：LOCAL-013 / NUI-88

当前独立副本0.210.4-ui.perf1，契约r57不变。SDE与全结果缓存、MCP紧凑JSON；UI候选复用、预览去重、简要事务回执与gzip。所有业务数值仍从公开引擎分析获得；保留完整性/null/来源，未新增规则。引擎和UI分别提交；下一次升级必须核对LOCAL-013并保留LOCAL-010～012。实测及验证见performance-20260920.md，用户体验待验收，未发布。

### 深渊工作台LOCAL-014 / NUI-90

0.210.5-ui.mutation1 / r58-ui-mutation1：mutation_workbench及CLI入口提供手动范围校验、联合概率/期望、实际分批试验。规则沿用既有SDE/随机模型。手动保存经公开接口核验，UI仅做单位展示与交互；随机命中保留可重放receipt。见abyssal-edit-and-trials.md与引擎docs/ui-local-changes.md。独立提交，不打包，不修改主库/历史候选。

### LOCAL-020 真实金鹏编辑性能续作

2026-09-20：0.210.11-ui.editperf1/r58成功验证复用及Dogma目标索引，完整金鹏分析golden一致。UI连续编辑共享公开分析hash；首次会话/保存后仍走原预览确认基准，后续以原生回执维护基准，不以展示快照替代事务身份。电容异步更新仅改变展示调度，完整/api/analyze仍保留。见NUI-108，剩余延迟仍开放。

### LOCAL-019 全技能性能

2026-09-20：0.210.10-ui.selectionperf1/r58将输出选择查询复用基础分析，保持原Aggregate及隔离缓存；FitLab子进程显式启用structuredContent-only成功回执，默认MCP客户端不变。见NUI-107与引擎台账；剩余全技能延迟仍开放，不以轻量样本宣称完成。

### LOCAL-018 导弹距离应用

2026-09-20：独立引擎0.210.9-ui.missilerange1/r58按已有飞行投影限制导弹静态应用距离，补精确曲线边界，公开结构不变。名义伤害保持原义，应用伤害超程归零，缺失距离明确不可用。UI只展示引擎采样与静态范围说明，详见NUI-105。

### LOCAL-017 导弹制导脚本

2026-09-20：独立引擎0.210.8-ui.guidance1/r58补齐导弹制导脚本准入、公开chargePolicy以及既有SDE修饰对已安装导弹的位置选择。UI按公开脚本策略跳过弹仓补满、弹量编辑和弹量提示，显示已装载脚本。无前端加成公式。验证见NUI-103及test_guidance_scripts.py；未打包。

### LOCAL-015 旗舰维修器准入

独立引擎0.210.6-ui.repair1修复SDE前置技能链识别，避免旗舰回盾器被误判不支持；原r58公开接口不变。主维护升级须保留此补丁，详见引擎docs/ui-local-changes.md。UI无计算绕过，NUI-91待用户验收。

2026-09-20：按用户确认的轻量预览范围新增NUI-99二维战斗回放，仅使用现有引擎公开CLI生成记录。未新增战斗机制或接口；独立文件避开并行本地化改动。详见sandbox-preview.md。

2026-09-20：接入独立引擎UI-LOCAL-016（0.210.7-ui.subsystem1/r58），允许战略巡洋舰未选齐四件的诊断编辑，严格合法性保持。子系统特性直接读取公开catalog_item。详见NUI-101。


### NUI-109：原子工作台接入

2026-09-20，UI-LOCAL-021，0.211.0-ui.workbench1/r59/38工具。公开fit_workbench合并编辑及摘要；完整接口保留，按需追溯，增量DOM，后台只读进程隔离排队。真实金鹏热进程首次316ms，20次重复状态中位125ms/P95 144ms；12个新状态99–271ms。冷启动和偶发尖峰仍开放。验证及方法见workbench-performance-r59.md。未打包、未推送，不纳入并行本地化修改。


### NUI-110：DSH agent MCP入口与负零修复

0.211.1-ui.agent1/r59，UI-LOCAL-022。原生38工具不变；独立agent-mcp-v1六工具提供批量检索、会话摘要、按需schema和结果引用。仅组合公开原生调用，无新公式。实际DSH负零校验及幽灵级MCP/CLI对照通过，合成battle start/status/result通过。详见agent-mcp-v1.md。DSH配置备份后切换、已重启，原会话保留；默认引擎构建同步，未重新打包。
