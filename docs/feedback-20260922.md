# 2026-09-22 反馈修复（未发布）

本轮只修改开发代码、验证及回传反馈；不打包、不推送 GitHub、不部署官网。

| 反馈 | 修复与验证 |
| --- | --- |
| FB-1CA8949BE73671FB | 下载按钮改用原生 popovertarget，使入口成为弹出面板的正式触发器，避免 light-dismiss 先关闭再被 onclick 重新打开。真实浏览器连续三轮打开/关闭通过。 |
| FB-BCC91EAF6F74D262 | 移除装配返回库时的 persistFit 调用。未保存内容仍保留为明确标注的本地草稿，不写入已保存装配。浏览器编辑备注后返回库，/api/save 请求为零，前后 /api/library 内容完全一致。手动保存控制器竞态回归通过。 |
| FB-AA7DD0CFD6275002 | 技能名称可点击查看官方描述中的每级加成，直接复用 /api/items 的同源说明；不凭空把每级加成线性推算成装配结果。射击学详情显示每级 2% 射速提升。 |
| FB-BE54471A5ABF5203 | 新增“创建 I～IV 技能对比角色”，确认后创建四个独立可编辑角色，供装配切换；编辑工具提供全部 I～V 预设及逐项微调。隔离状态中创建四个角色并读取角色 API 核对成功。 |
| FB-873B4B6603D157FF | 技能预设和逐项编辑按官方 requiredSkill1～6 / Level 属性递归补齐前置技能，不降低官方要求。验证 IV 级目标技能保留 IV，要求 V 的前置技能补 V；所有实际技能的 I～V 预设闭包验证通过。 |

测试使用独立 output/feedback0922-state，未在用户角色库创建测试角色。Playwright 脚本和截图位于 output/playwright/feedback0922.cjs、feedback0922-skill.png。执行 node test_character_skill_levels.mjs、node test_fit_save_controller.mjs、node scripts/check-ui.cjs，全部通过。

本次使用既有官方属性和物品描述接口；不新增计算公式或引擎协议。全 I～IV 的名称表示目标等级，必要前置技能会高于该值，创建确认和编辑状态中均说明。对比角色用于切换装配计算，不声称新增并排装配对比界面。
